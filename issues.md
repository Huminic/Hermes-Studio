# Huminic Studio — Issues

**Last verified:** 2026-07-17

### [DEMO — Huminic Motors sales demo, IN PROGRESS 2026-07-17] Provisioning state + open items
- **DONE:** demo-safe comms guard (checkCommGate rule 1a, 15 tests, campaign-bounded via DEMO_TEST_RECIPIENTS); demo-capture/demo-login/demo-callback endpoints; static site (webroot); huminic-motors tenant cloned from serra-honda + rebranded (40 wiki nodes, 0 residual Honda, Huminic Motors dealership facts, widget slug nodes). **CHAT LIVE + verified** on studio.huminic.app (huminic-motors widget returns "1200 Innovation Parkway, Austin TX" + hours). External: **Vapi "Anastasia - Huminic Motors" = `bc56b1bb-b4be-41e9-aaee-80ec3551e5a2`** (OpenAI voice — Vapi legacy voices blocked for new assistants); **Tavus persona "Anastasia" = `p9d497a88cfd`** (wired to huminic-motors studio.yaml video_persona_id).
- **DEBT / TODO:**
  - **Vapi OWN-mode is a scaffold** (`messaging-adapters.ts dispatchVapi` OWN branch does NOT pass a per-profile assistantId/phoneNumberId — only SHARED mode uses env VAPI_ASSISTANT_ID). To place a huminic-motors AI callback with Anastasia WITHOUT affecting other stores, complete the OWN-mode path to read a per-profile assistant id + phone from the profile env/config. (deploy-gated code change)
  - **No unused Vapi phone number** — all 8 are assigned. Operator decision: buy a new number (real charge) or reuse "Elliott - Test Assistant" +18392729080.
  - **TextMagic** — huminic-motors channel_credentials.sms:own; needs a provisioned number/subaccount (operator).
  - **DEMO_TEST_RECIPIENTS** not set (operator provides tester phone/email).
- **GATED (operator GO):** PR merge + prod deploy (guard/capture/login/callback go live); DNS create demo.huminic.app → 150.136.6.207 via huminic-app-dns.js; first real outreach send.
- **NOT yet done:** full Playwright E2E (SMS/callback need deploy + provisioning); fresh code-reviewer independent verification.

### [P1 — RESOLVED 2026-07-16: fixed, deployed (PR #86 → main aa8db763f), independently CERTIFIED live on all 5 stores] Public web widget fabricated hours/address — canonical fact node crowded out of top-3 retrieval (ALL stores)
- **RESOLUTION (2026-07-16, operator GO "deploy and certify"):** `recallWidgetGrounding` (commit 5c8390f68) pins each store's canonical `dealership/hours-location-contact` node on hours/address/phone/location intent regardless of keyword rank; draft nodes never ground; tightened intent regex; path dedup. Merged PR #86 → main `aa8db763f`, Coolify-deployed (both containers on the new image, agent healthy). **LIVE-CERTIFIED on the deployed public endpoint (studio.huminic.app/api/public/widget-chat) for ALL 5 stores** — each returns its CANONICAL address+hours, ZERO fabrication (serra-honda 40941 US-280; serra-nissan 80 James Payton Blvd — was the fabricated "1500 Covington Pike, Memphis TN"; tony-serra-ford 40905 Hwy 280; ford-of-columbia 1350 Nashville Hwy; hyundai-of-columbia 1370 Nashville Hwy). serra-nissan holds across two phrasings. Independently certified by a fresh reviewer against live prod (deploy tag + 5/5 canonical + no fabrication + studio serving). Full suite 1190 pass; build green. Original finding retained below for the record.
- **Symptom (live, reproducible on studio.huminic.app):** the public sales widget answers hours/address questions with a **fabricated location**. serra-nissan `/api/public/widget-chat` (slug `serra-nissan-sales-chat`) returned *"1500 Covington Pike, Memphis, TN 38122"* (twice, two phrasings) — an address that appears in **no** wiki node. The canonical node `company-wiki/dealership/hours-location-contact.md` (status:canonical, correct: **80 James Payton Blvd, Sylacauga AL**, Sales M–Sa 9–7) was **not injected**, so the model answered from its own training and invented a plausible Nissan address. Anti-fabrication invariant violated at runtime, customer-facing.
- **NOT nissan-specific — systemic.** serra-honda's widget returned the CORRECT canonical address for "hours and where located" but that was a 1-rank fluke. Replicated the exact `recallCompanyWikiTop` scoring (`knowledge-mcp-handlers.ts:101`; keyword score, head×2/body×1, keep top-3 with score≥3):
  - honda "hours and located": hours node score 10, **rank #3** → injected (barely) → correct answer.
  - honda "street address": hours node score 3, **rank #16** → NOT injected → honda **also** fabricates for this phrasing.
  - nissan "hours and located": hours node score 9, **rank #4** → NOT injected → fabricates.
  - nissan "street address": hours node score 4, **rank #11** → NOT injected → fabricates.
- **ROOT CAUSE:** the verbose conversational-**play/process** nodes seeded for the autonomous-reply grounding path (`sales/*-play.md`, `trade-in-process.md`, `test-drive-and-appointment-scheduling.md`, `00-start-here/welcome.md`) incidentally match generic query words ("your/and/where/located/hours") and **out-rank** the single `dealership/hours-location-contact` fact node. Because the widget keeps only the **top-3** hits, the fact node falls out and the model hallucinates. The seeding (good for the autonomous path via `recallForAgent` domain+canonical filtering) has an unintended side effect on the widget's simpler `recallCompanyWikiTop` retrieval, which has **no fact-node priority, no domain filter, and no intent detection**.
- **NOT caused by this deploy.** `widget-chat.ts` retrieval + the play nodes both pre-date PR #85. The prior "live widget-grounding E2E PASS for Serra Nissan" claim was too narrow (one lucky query / or tested the autonomous `recallForAgent` path, not the public widget's top-3 `recallCompanyWikiTop`).
- **Exposure:** the `/w/<slug>` public endpoints are live and answer, but per operator the store widgets are "ready to deploy to dealer sites upon convenience" — so real-customer exposure is limited *if* the widgets are not yet embedded on live dealer sites. Needs operator confirmation of which widgets are embedded.
- **FIX OPTIONS (operator to choose — touches SHARED widget retrieval, affects every store; NOT applied unilaterally):**
  1. **Pin the dealership fact node on hours/address/phone/location intent** — when the query matches that intent, always inject `dealership/hours-location-contact` regardless of rank. Lowest-risk, targeted; recommended.
  2. **Raise fact-node weight in `recallCompanyWikiTop`** — boost `node_type: knowledge` / `dealership/*` so fact nodes out-rank prose plays; simple but changes ranking for all queries.
  3. **Increase injected hits (top-3 → ~6-8) and/or add the `recallForAgent` canonical+domain filter to the widget path** — unifies widget with the autonomous grounding; more work + more context tokens.
  4. **Widen the score threshold + de-weight long play nodes** (length-normalize). Broadest change, highest regression risk.
- **Recommendation:** Option 1 (intent-pin the contact node) + a regression test asserting hours/address/phone queries inject the canonical node for every store; then re-run the live widget probe per store. HOLD for operator GO before touching shared retrieval.

### [P2 — found 2026-07-15, INVESTIGATED + CONCLUDED 2026-07-15: root cause = Resend 10/s rate limit, ALREADY FIXED 2026-07-10] Lead-notification email failures (serra-honda comms_log 110 error / 72 ok)
- **ROOT CAUSE (proven with hard evidence):** the Huminic Resend API key enforces a hard **10 requests/second** limit per key. Proof: 20 concurrent Resend API calls returned **exactly 10× HTTP 200 + 10× HTTP 429** (`ratelimit-limit: 10; ratelimit-policy: 10;w=1; ratelimit-remaining: 0; retry-after: 1`) — no email sent to prove it. The old lead-notification path fired every dealer recipient at once via a **6-way `Promise.all`**; when recipient fan-outs (5-6 emails/lead) and overlapping lead events collided in the same second the burst exceeded 10/s → Resend returned 429 → the send path recorded `outcome=error`.
- **Ruled OUT (evidence):** (a) domain/sender — `huminic.ai` is `status: verified`, `sending: enabled` (Resend `/domains`); (b) invalid recipients — every recipient has a MIX of ok+error (victoria 1ok/32err, sdew 13ok/3err, leads@serrahonda.co 25ok/8err), so no address fails 100%; a fixed bad-address or allowlist theory is impossible. The failure correlates with **timing/burst**, not recipient.
- **ALREADY FIXED (2026-07-10, before this investigation):** commits `5c8e1047c` ("serialize lead-notify fan-out + retry rate-limit + persist reason (P0-2)") and `8329eea17` ("serialize the notifyActiveConversation fan-out too + backoff"). The fan-out is now **sequential with `FANOUT_SPACING_MS=400` spacing**, `sendViaResendRaw` retries on `rate.?limit|429|too many requests` with backoff (`RESEND_MAX_ATTEMPTS=3`), and the Resend error reason is now persisted.
- **DATA CONSISTENT WITH RESOLVED (causal caveat below):** all 110 serra-honda email errors fall **2026-06-24 → 2026-07-10 08:15**; **zero email errors after 2026-07-10**. Post-fix, a **6-recipient** fan-out on 2026-07-12 delivered **all 6 recipients OK, spaced ~500ms apart — including `victoria@…` which was 0ok/32err before that send**, directly supporting the timing/burst cause over deliverability. Same account-wide pattern (shared key) on ford-of-columbia (11err/31ok) and serra-nissan (9err/27ok), all historical. The old error rows carry no error string because "persist reason" landed in the same 07-10 fix — future failures WILL persist the reason (visible in the D9 Activity page).
- **CAUSAL CAVEAT (honest bound on the evidence):** the last error (07-10 08:15) PREDATES the fix commit (07-10 09:15/09:45) by ~1h, so the error series had already stopped before the code shipped — the "zero after" boundary is *consistent with* the fix but does not by itself prove causation (a lull in lead volume could contribute). The 10/s-429 attribution for the 110 historical rows rests on the live rate-limit probe (20 concurrent → 10×200/10×429) + the code mechanism (un-throttled 6-way fan-out), NOT on stored error reasons (which those rows lack). What IS proven: the 10/s limit is real, domain/recipients are ruled out, the code now serializes+retries 429, and the one clean post-fix 6-way fan-out. What is NOT independently proven from data alone: that each of the 110 old failures was specifically a 429.
- **REMEDY: none required — already in code + deployed.** Classification per the goal: this was a **code** defect (un-throttled fan-out), fixed in code; NOT config (domain verified), NOT data (recipients valid). No email-sending config or recipient change made or needed (gate respected).
- **OPTIONAL follow-ups (low priority, no recurrence evidence):** (1) if concurrent lead bursts ACROSS stores ever re-trip the shared 10/s, add a single cross-event token-bucket limiter in the central-mcp Resend path (all stores share one key); (2) the D9 Activity page shows all-time history, so the pre-07-10 failures still display — consider a date filter or a "resolved before <date>" hint so old failures aren't misread as current.

### [P0? — found 2026-07-13, needs operator intent confirm] New leads get NO instant SMS — first touch is a 24h "are you being taken care of?" check-in
- **Evidence (VIN-authoritative + live DB):** VinSolutions shows **93 leads created 2026-07-09→07-13**, almost all fresh `leadType: INTERNET`, `leadStatus: ACTIVE_NEW_LEAD`, `leadGroupCategory: NEW` (~15-18 good new leads/day). Brand-new internet leads flow continuously — NOT re-engagement of old leads.
- **The gap:** the **"Instant SMS for new leads"** automation (`4ebcf94c`, trigger `new_lead`, wait_hours 0, **status=active**) has fired **only twice ever, last 2026-07-02**. The **"24-hour follow-up for all leads"** (`6b7aed1e`, `lead_followup`) fires daily (~12/day). Traced 5 recent lead contacts: each has exactly ONE thread — the 24h check-in — with **no prior instant SMS**. So a brand-new lead's first (and often only) contact is *"we wanted to check in — are you being taken care of?"* ~24h later, wording that wrongly implies a prior conversation that never happened.
- **"Supposed vs actual" (operator's question):** ~15-18 new leads/day SHOULD each get an immediate response and are NOT — they only get the delayed check-in. That is the real outbound gap (the held/failed/deferred queues are all empty).
- **ROOT CAUSE — CONFIRMED (2026-07-13):** the outbound cadence is driven by two catch-up scripts run from the host crontab. `scripts/catchup-followup.ts` (24h check-ins) IS scheduled — `0 13-23,0-2 * * * cron-catchup-followup.sh` (hourly, in-window), plus the branch-proof `hermes-ops/followup.sh`. `scripts/catchup-immediate.ts` (the IMMEDIATE first-touch to `ACTIVE_NEW_LEAD` leads) is **scheduled NOWHERE** — no crontab entry, no `cron-catchup-immediate.sh` wrapper, and the 07-10 crontab backups confirm it was never scheduled. So new leads get the follow-up (cron runs it) but never the instant text (nothing runs it). The 2 historical instant runs (last 07-02) were likely manual `--send` runs. `catchup-immediate.ts` is SAFE-BY-DEFAULT (dry-run unless `--send`; every send passes comms-gate: kill-switch/prelaunch/business-hours/blacklist/rate-limit).
- **FIX (needs operator GO — real customer sends + infra):** add a `cron-catchup-immediate.sh` wrapper (mirror the followup one) + hermes-ops branch-proof copy, and a crontab entry to run `catchup-immediate.ts --send` on the in-window schedule. This STARTS ~15-18 real first-touch SMS/day → needs (1) operator intent confirm (do new leads get instant SMS?), (2) GO for real sends, (3) infra/crontab change coordinated per sysadmin rules. Recommend a controlled first run (dry-run → verify recipient list → `--send` to a small batch) before cron'ing.
- **REFINEMENT (2026-07-13, design-intent traced from git + config backups):** the "missing cron" is one layer; the deeper truth is an **incompletely-activated staged rollout — not broken code.** Design (`work/2026-07-08-phase2-activation-staged.md`, operator-confirmed then): the intended driver is the in-process tick `COMMS_TICK_ENABLED` (runs `runDueWork → tickVinWatcher → processNewLead`, the ONLY code path that auto-fires msg-1 + enrolls msg-2). It was deliberately kept OFF; first-touch ran on **Path A = manual/supervised** (`catchup-immediate --send`, in-window). **Path B = continuous immediate** was **explicitly deferred** with an OPEN prerequisite **D6** (backlog.md): `processNewLead` fires the immediate instantly and does NOT yet honor the tight 6–8pm/8am window (only the broad 08:00–21:00 comms-gate) — must be wired before continuous immediate is compliant. Config-backup timeline: `vin.watcher.enabled` flipped **false→true on Jul 1 18:00** (someone began Path B) but `COMMS_TICK_ENABLED` was **never set** → the watcher flag is **inert** (nothing calls it). So msg-1 has had **no live driver since Jul 1**; the last 2 real sends (Jul 1/Jul 2) were manual Path-A runs. msg-2 kept flowing on its own dedicated cron (independent of COMMS_TICK). This reconciles "both were live last week": msg-2 genuinely was; msg-1's config *read* enabled but had no driver. Automations config = `{}` (no NLP; first-touch is the fixed `renderImmediate` template; `agent_id:caroline` governs only the inbound-reply layer). UI corroborates: Automations tab shows "Instant SMS…Active · last sent 7/2/2026" vs "24-hour…Active · last sent 7/13/2026".
- **CATCH-UP AUDIENCE (dry-run of real code path, 7d):** 156 VIN leads polled → only **3 still `ACTIVE_NEW_LEAD` + un-texted + not agent-handled**: James `+13346524599` (zero touches), Kristen `+12562674043` (got msg-2 not msg-1), Krista `+13345310901` (got msg-2 not msg-1). Vapi/Tavus-born leads correctly excluded from msg-1 (avoid doubling AI interactions); VIN + widget-non-agent leads included (operator-confirmed).
- **FIX OPTIONS (operator to choose; unchanged sends still gated):** (1) **dedicated immediate cron** mirroring the proven follow-up cron — the `catchup-immediate.ts` script already enforces the 6–8pm/8am window, so no D6 needed; lowest risk; generalize per-profile (any dealer with an active `new_lead` automation), no Serra-Honda special code. *(recommended)* (2) turn on `COMMS_TICK` (original in-process driver) — real-time, but **requires D6 first** + careful decommission of the overlapping crons; defer to a planned migration. (3) keep manual — rejected as the forward fix.
- **RESOLVED 2026-07-13 (operator-approved Option 1 + priority catch-up executed):** Operator chose the **generic immediate cron** (not COMMS_TICK). Built on branch `fix/lead-engagement-immediate-driver`: `scripts/cron-catchup-immediate.sh` (repo, generic — iterates `IMMEDIATE_PROFILES`, self-skips dealers without an active new_lead automation) + branch-proof `hermes-ops/immediate.sh`. Activated on host crontab `0,30 13,14,23,0,1,2 * * * hermes-ops/immediate.sh` (DST-safe band; the TS script self-guards the 6–8pm/8am CT immediate window + dedups via `automation_runs`). Tested live: in-window send proven (catch-up below); out-of-window refuse proven (exit 1, no send); heartbeat honest. **First live window run: 6pm CT today.** Both automations now driven (msg-1 = immediate cron; msg-2 = followup cron). NOTE: still NOT the in-process COMMS_TICK path (`vin.watcher.enabled:true` remains inert by design); D6 stays open for a future real-time migration.
- **PRIORITY CATCH-UP SENT (operator-GO'd "now, supervised" — legal A2P window, prelaunch-lock scoped to the exact numbers, comms-gate enforced):** msg-1 first-touch delivered to **Kristen `+12562674043`** + **Krista `+13345310901`** (both got msg-2 but not msg-1, no sales follow-up). Verified: `automation_runs` 4ebcf94c +2 `sent` @16:05Z, `last_triggered_at` updated, hub messages present. **James `+13346524599` correctly DROPPED** between the first poll and send — his VIN status moved past ACTIVE_NEW_LEAD (a salesperson followed up), so per operator criteria he must NOT get a message. Shawn `+12052539897` also excluded (rep took the thread over = follow-up done; out of our scope — operator-confirmed boundary, see [[feedback-rep-takeover-is-followup]]).
- **DEBT recorded (not part of this fix):** (a) `hermes-ops/followup.sh` heartbeat had a bash `$?`-clobber bug (a `$(date)` in the same echo reset `$?` to 0 under bash, so it logged `exit=0` regardless of real send/refuse) — **FIXED** here (capture `rc=$?` first); same fix applied to the new immediate wrappers. (b) `followup.sh`+`holds.sh` each have TWO crontab entries (repo `scripts/cron-*.sh` + branch-proof `hermes-ops/*.sh`) doing the same job — harmless (ledger dedups) but redundant; immediate deliberately added only ONE. Cleanup = consolidate on the branch-proof `hermes-ops/*` set and drop the repo-cron entries.
- **Status:** RESOLVED (msg-1 driver live + catch-up sent + verified). Repo file `scripts/cron-catchup-immediate.sh` on branch `fix/lead-engagement-immediate-driver` — needs commit + PR. Follow-ups: verify first live 6pm-CT window run; cron-dedup cleanup; D6 (real-time COMMS_TICK) if/when operator wants instant first-touch.

### [P1 / STRANDED PROSPECT — found + root-caused 2026-07-13] A human-taken-over thread has NO safety net when the human never follows up
- **Symptom (live, serra-honda):** thread `6a646c1b` (Shawn, `+12052539897`). Customer replied **"I wish. I needed a vehicle"** at **2026-07-11T20:57Z (15:57 CT, in-window)** — a buying signal — and got **no reply for ~56h**, no AI, no human. Found by live audit; confirmed the ONLY genuine miss in a full sweep of all 17 inbound SMS threads in the live window (7 were correct STOP opt-outs, 8 replied, 0 stuck holds; 1 excluded internal test line `+14126546500` = GoLiveTest/duanekwells@gmail.com).
- **ACTUAL ROOT CAUSE (verified — my first two hypotheses were WRONG; corrected here):** NOT a silent-catch throw, NOT the max-turns cap, NOT an automation false-takeover. The thread was **legitimately human-taken-over**: `thread_takeover` (lives in `brain/brain.db`, NOT messaging-hub.db — why an earlier query missed it) has a row `assigned_to='serra-honda'`, `ts=2026-07-11T17:01:49.236`. At 17:01 someone in the **Teambox UI logged in as the store admin account (`username='serra-honda'`)** hit **take-over** (`threads.$threadId.assign.ts` action=take_over → `assignThreadToHuman`) then sent a **manual reply** ("Hey Shawn! …confirm you were taken care of", author=`serra-honda`, `ts=17:01:49.996`, via `threads.$threadId.reply.ts` which REQUIRES prior takeover, line 51). Human takeover **permanently stops the AI** for that thread by design (`maybeAutonomousReply` returns `[]` at `agent-autonomous-reply.ts:535` via `isHumanAssigned`). So when Shawn replied at 20:57 the AI **correctly** stayed silent — but the human who took over **never followed up**. `agent_reply_jobs` has no row for the 20:57 msg precisely because the human-assigned early-return happens before enqueue (line 535 << 566).
- **Takeover-origin trace (2026-07-13, read-only):** all 3 `thread_takeover` rows are `assigned_to='serra-honda'` and each has a human-style manual reply sent AT the takeover instant by `author='serra-honda'` — a PERSON (named "Shelby" in one: "Hey, this is Shelby with the Customer Service…"; another: "What is your first and last name?") working conversations from the **shared store Teambox login**, NOT automation. 2 are web-chat threads, 1 is Shawn's SMS. CONFIRMED: the takeover feature works as designed; this is a human-process issue (a rep took over via the shared login and didn't follow up), not a false-takeover code bug. Shared login = no individual accountability for the drop.
- **The real gap (design):** once a thread is human-assigned, a subsequent customer inbound that the human ignores is **stranded silently** — the AI won't touch it (human assigned) and nothing escalates if the human forgets. No SLA/Sentinel check covers "human-taken-over thread with an unanswered inbound aging past N hours." That's the "no prospect left behind" hole. (Contributing: the takeover was by the generic shared `serra-honda` admin login, not a named rep, so accountability is diffuse. Only 3 takeover rows exist, all `serra-honda`.)
- **Latent robustness note (separate, keep):** the webhook DOES swallow reply-path errors silently (`textmagic.$profile.ts:293-301` bare `catch {}`, no log/job/hold). That did NOT cause Shawn, but it means a real transient error would also drop a prospect invisibly — worth hardening (log + recoverable marker) independent of this incident.
- **FIX DEPLOYED + Checker-verified 2026-07-13 (operator GO'd; PR #84, merge `7bffc9702`, live):** enhanced `threadSlaCheck` (`sentinel.ts`) — a HUMAN-OWNED (taken-over) thread whose last message is an unanswered inbound past the 30m SLA now emits a NAMED per-thread finding `agent-health:<profile>:stranded-takeover:<threadId>` (contact in title, critical >4h) with **NO 24h abandon ceiling** (a human owns it → must never go dark); STOP/opt-out last-messages excluded (`STOP_RE`); non-owned behavior (aggregate `:unanswered` + 24h ceiling) unchanged. Added `isHumanAssigned` to SentinelStore (wired to thread-takeover). Monitoring-only (no sends/writes). **Full test suite 1154 pass + build green.** Checker independently verified code + tests + no-regression + LIVE logic: across 180 open serra-honda threads it flags EXACTLY thread 6a646c1b/+12052539897 (32.4h, CRITICAL), 0 false positives (the other 2 takeover threads correctly not flagged — their last msg is outbound). STILL PENDING (separate, not yet built): (2) re-alert the assignee on a new inbound to a taken-over thread (notifyActiveConversation default-off); (3) harden the webhook silent `catch`.
- **Recovery (operator-directed 2026-07-13):** ONE prospect to recover — Shawn (`+12052539897`). Since the thread is human-assigned, recovery = release the abandoned 56h takeover, then let the gated AI reply fire (in-window) or defer to next A2P window (`checkCommGate` re-verifies opt-out/window/consent). Preview of the AI recovery reply (generation-only, verified sensible + grounded): *"I'm sorry you're still waiting. I'll let our sales team know you're looking for a vehicle and have someone reach out with options — is today or tomorrow better for a quick call?"* NO send without operator GO + Checker verification.
- **Status:** OPEN. Root cause CONFIRMED. Full sweep DONE (1 miss, 0 stuck holds). Recovery + fix pending operator GO + Checker.

### [P1-4-related / data defect] Vapi voice assistants quote WRONG dealership address/phone for several stores
- **Background (found 2026-07-11 while sourcing hours via `vapi_list_assistants`):** several stores' Vapi voice-AI system prompts carry incorrect DEALERSHIP INFORMATION vs the dealer's own website:
  - **Tony Serra Ford** ("Georgia") — prompt has **Serra Honda's** address (40941 US Highway 280) AND Honda's phone (256) 245-5000. Real: **40905** Highway 280, Sylacauga; Sales **(256) 401-0218**.
  - **Ford of Columbia** ("Savannah") — prompt has address **1229** Nashville Hwy + phone **(931) 369-2815**. Real: **1350** Nashville Hwy; **(931) 388-2463**.
  - **Serra Nissan** ("Magnolia") — prompt facts match the site (80 James Payton Blvd, 256-245-5000). OK.
  - **Hyundai of Columbia** ("Elizabeth") — prompt has NO address/phone block at all.
- **Impact:** a caller to the Ford stores' voice AI can be told the wrong address (Honda's lot / a wrong street number) and a wrong callback number. Customer-facing correctness defect on the voice channel.
- **Correct values:** see the 5-store hours matrix in the P1-6-followup entry (all website-verified + Checker-confirmed 2026-07-11).
- **Fix path:** update each Vapi assistant's system prompt (provider dashboard). The central-mcp toolset is READ-ONLY for Vapi (`vapi_list_assistants` only; no `vapi_update_assistant`), so this cannot be done via API — same provider-side surface the operator DEFERRED for P1-4/P3-11. Bundle this correction into that later Vapi/Tavus wiring phase. The studio company-wiki hours nodes are already correct + grounded, so the studio (SMS/chat) channels are unaffected; only the provider-hosted voice prompt is wrong.
- **Status:** OPEN — DEFERRED to the later provider-wiring phase (no API path). Recorded so it is not lost. [[paused-phases-resume-after-remediation]]

### [DATA-CORRECTNESS / P1-5-related — found 2026-07-13] Dashboard "Video Sessions" + "Web Chats" count MESSAGES, not sessions (inflated vs. "Conversations")
- **Evidence (code + live DB, serra-honda 30d):** `src/server/dashboard-metrics.ts` `buildAiActivityTab` — `Conversations` = `conversationCounts` = **thread count** (146, matches DB threads exactly). But `Video Sessions` = `chTotal(video)+chTotal(tavus)` and `Web Chats` = `chTotal(chat)`, and `chTotal` with no direction = **inbound+outbound MESSAGE count** (line 516). Live DB: video in+out = 72 messages across only **16 video threads**; chat in+out = 101 messages across only **19 chat threads**. So the cards show **Video Sessions 72 (real sessions 16, ~4.5×)** and **Web Chats 101 (real 19, ~5×)**.
- **Why it's a defect:** sitting next to "Conversations" (which correctly counts threads/sessions), a dealer reads "Video Sessions"/"Web Chats" as session counts — but they're message volumes. Overstates video calls and web chats. This is precisely the "no inflation" bar P1-5 exists to hold.
- **Correct (verified accurate to their definitions):** Conversations 146=threads ✅; Calls Received 6=voice+vapi inbound ✅; Calls Made 6=outbound ✅; Texts Sent 152=sms+textmagic outbound ✅; Emails Sent/Hunches/InfoStore all consistent with code.
- **Caveat (separate):** "Emails Sent 0" counts only hub-channel email; the real lead-notification emails (ADF/BDC) go via a different path and are NOT counted → understates email activity. Consider labeling/scope note.
- **Fix options:** (a) relabel to "Video Messages"/"Web Chat Messages" (cheapest, honest); or (b) change the value to per-channel THREAD counts so "Sessions"/"Chats" mean sessions (matches "Conversations"). Operator to pick. NOT yet done.
- **Not yet cross-checked:** Funnel/Pipeline `total_leads`=294 + `sold`=25 vs VIN (VIN queries intermittently time out); the "needs supplemental data" gating on unsourced stages IS working (no fabrication).
- **Status:** OPEN — data-correctness/labeling. Recorded 2026-07-13.

### [FEATURE — built 2026-07-13, on branch, needs deploy] Custom Reports suite (7 reports) + dashboard data fix
- **Dashboard fix:** `dashboard-metrics.ts` — "Video Sessions" + "Web Chats" now count SESSIONS (distinct threads), not in+out messages (was ~4.5×/5× inflated). Tested (a 4-message session = 1). Deploy needed to reach the live dashboard.
- **Reports (generic per-profile, HTML `report` artifacts via `POST /api/customer/report-generate`):** all built + unit-tested + full-suite green (1173 pass) + build green:
  1. missed-opportunities (hub/holds/failed-sends; excludes rep takeovers + opt-outs)
  2. lead-source-detail (ingested ROI; honest no-report state)
  3. salesperson-effectiveness (ingested KPI)
  4. ai-conversation-insights (transcripts + injectable LLM via new `completeChat` in dashboard-ask.ts; honest unconfigured state)
  5. data-issues (undeliverable/unassigned/stuck-holds/fragmented)
  6. store-performance-trend (trends across ingested report periods; needs ≥2 periods)
  7. competitor (source-agnostic; accepts competitor data from body/federated-search; honest "connect the source" state — NEVER fabricates pricing/ads)
- **PENDING (operator):** (a) DEPLOY the branch (all-stores restart = GO) so the fix + reports go live; (b) commit/PR; (c) point me to the background FEDERATED SEARCH so #7 gets live competitor data; (d) #6 needs ≥2 uploaded report periods to show a trend; (e) reports are reachable via the API + artifacts surface — a customer-console nav entry/button to trigger them is a small follow-up if desired.
- **Status:** OPEN — feature complete on branch `fix/lead-engagement-immediate-driver`; not live until deployed.

### [PLATFORM COMPLETION goal — in progress 2026-07-14, branch fix/lead-engagement-immediate-driver / PR #85]
- **A1 (P3-9) DONE+verified:** serra-honda `conversation-approach.md` short-form dangling refs → full-form on the LIVE volume; all targets resolve, 0 remaining short-form refs. NOTE: the 4 non-honda stores already have correct full-form refs but are MISSING the `agents/caroline/role` node (real orphan) → folded into D10.
- **A3 (P2-7) DONE+tested (PR):** `dispatchOutbound` now records SMS/voice send OUTCOMES to comms_log (was email-only) → a FAILED customer text is now visible to the channel-agnostic `notificationsDeliveryCheck` (1h) → Sentinel email. Pure `commsOutcomeRowFor` helper unit-tested. Full suite 1176 pass.
- **A4 (P1-6 followup) DONE+verified:** non-honda PUBLISHED wiki was seeded from a Honda template ("Serra Nissan is a full-service **Honda** dealership", "genuine Honda parts", 13 mentions/store). Brand-swapped per store on LIVE volumes (Nissan/Ford/Ford/Hyundai), skipping source_of_truth provenance lines; 0 residual body-Honda; per-store `company-wiki.bak-p16-*` backups. Grounding reads files directly (no re-index needed).
- **B5 DONE (already canonical):** serra-honda hours node verified canonical + correct on live volume.
- **A2 (P3-13a) IDENTIFIED — GATED (destructive DB op, needs GO or deploy-restart window):** 18 leaked-prompt video rows (content LIKE 'system:%', the "system: Core Identity…You are Caroline" 20K dump), all one-time Jun 10-11, already filtered from customers by PFF-007. Per store: honda 4, nissan 3, tony-ford 2, ford-columbia 6, hyundai 3. NOT deleted — a raw DELETE on live prod DBs the app holds open is a destructive op; run during the gated deploy restart or with explicit GO.
- **B6 DONE (operator decision applied 2026-07-14, config only — validated write via updateNotificationRouting, backups studio.yaml.bak-b6-*):** Serra Nissan + Tony Serra Ford = replicate Serra Honda internal team (Victoria, Don Wood, Cage, Duane, BDC sdew@) + KEEP each store's OWN DMS ADF (leads@serranissanofsylacauga.net / leads@tonyserraford.net) — did NOT copy Honda's DMS (would misroute to Honda's CRM; operator confirmed the ADF-keep). Ford of Columbia + Hyundai of Columbia = Sam Mayfield + Duane + Durran Cage, NO ADF (operator confirmed both Columbia stores identical + not using ADF/XML). Verified read-back + studio.yaml still parses.
- **WIDGET DEPLOYABILITY VERIFIED (operator ask 2026-07-14):** all 5 stores have the full unified widget configured (enabled; channels chat + callback[Vapi] + form + video[Tavus] all true; each store its OWN video_persona_id + agent name Caroline/Magnolia/Georgia/Savannah/Elizabeth). Deployable embed loader serves live HTTP 200 per store (studio.huminic.app/widget/dealer/<profile>.js, ~10.6KB). Storefront UI (shared code) shows the copy-paste embed snippet per store. Public widget grounding live-verified (serra-nissan → Nissan). Each store has an operating universal widget ready to hand to its website.
- **D8 DECISION NEEDED:** report-upload already EXISTS in studio InfoStore→Data and AUTO-INGESTS CSV by loaded profile. Gap: PDF is stored-not-parsed. No PDF lib in package.json; codebase deliberately avoids parser deps (XLSX intentionally unhandled). Reliable PDF *table* extraction is hard/error-prone. P1-5 acceptance allows "PDF either parses OR the UI states export-as-CSV." RECOMMEND the export-as-CSV guidance + prominent button (no risky dep) unless operator wants a best-effort PDF-text dep added.
- **D10 REVISED:** the "ungrounded" non-honda stores actually already HAVE rich wikis (00-start-here/03-sales/05-teams + sales/policy nodes). Real gap = the missing `agents/caroline/role` node (orphan) + verify live grounding. Not a from-scratch seed.
- **D8 DONE+tested (PR):** studio report-upload already auto-ingests CSV by profile; added `uploadResultNote` (surfaces "N rows now power your dashboard"; PDF→export-as-CSV guidance, no false parse claim — matches P1-5 acceptance), prominent "Upload report (CSV)" button. Unit-tested.
- **D9 DONE+tested (PR):** new Activity nav page — per-store feed of comms/notification/send events from comms_log (newest-first) + 24h summary (sends+failures by channel). `activity-log.ts` reader + `/api/customer/activity` (auth-gated) + `CustomerActivityRenderer` + registry/nav wiring. Reader unit-tested; nav-add can't break other stores (isMenuEnabled defaults unknown ids true).
- **D10 DONE:** all 5 stores now have `agents/caroline/role.md` (the missing orphan target); nissan+ford relationship targets resolve.
- **FRESH INDEPENDENT REVIEWER (general-purpose agent, not the implementer): VERIFIED ALL.** Reproduced build green + 1184 pass + 1 skip and every live-data claim (A1 full-form refs, A4 0-body-Honda + backups, D10 role.md on all 5). Found 1 minor bug — report artifact description hardcoded to missed-opportunities blurb for all 7 reports → FIXED (per-report descriptions). No fabrication/regressions.
- **C7 READ-ONLY PREP DONE (exact before→after confirmed from live Vapi read, `vapi_list_assistants`):**
  - **Georgia — Tony Serra Ford** (id `2203b188-a549-417b-ab33-075766e1b5c1`): prompt has `40941 US Highway 280` + `(256) 245-5000` = SERRA HONDA's info → fix to `40905 Highway 280, Sylacauga AL 35150` / Sales `(256) 401-0218`. Add Serra pronunciation.
  - **Savannah — Ford of Columbia** (id `6d12a8fa-0ed0-4ec1-bfdb-e84587ff86c0`): `1229 Nashville Highway` + `(931) 369-2815` → fix to `1350 Nashville Hwy, Columbia TN 38401` / `(931) 388-2463`.
  - **Elizabeth — Hyundai of Columbia**: NO dealership-info block at all → add `1370 Nashville Hwy, Columbia TN` block.
  - **Magnolia — Serra Nissan** (id `6216451c-e0a3-43d0-aece-ae382bd8df25`): `80 James Payton Blvd` + `(256) 245-5000` = CORRECT; only add Serra pronunciation.
  - **Serra pronunciation**: NONE carry a "Serra"→"SAIR-uh" hint (STT hears "Sarah"); add to the Serra-branded ones.
  - **C7 APPLIED + VERIFIED 2026-07-15 (operator provided Vapi access):** Vapi private key VAPI_PRIVATE_KEY (from nexxus2.2_replit/.env) used for direct Vapi API PATCH. **CRITICAL: the assistant IDs I'd recorded earlier from the central-mcp read were SCRAMBLED** (id mis-attributed to wrong store — exactly the "agents mix them up" the operator warned of); re-fetched correct IDs from the authoritative Vapi API before touching anything. CORRECT ids: Georgia/Tony-Serra-Ford ad478eb2, Magnolia/Serra-Nissan 2203b188, Savannah/Ford-of-Columbia 6216451c, Elizabeth/Hyundai 6d12a8fa, Caroline/Serra-Honda 90a876c0. Applied surgical prompt fixes (backups /tmp/vapi-bak/*.json), each PATCH HTTP 200, all verified via read-back: Tony Serra Ford 40905/(256)401-0218 + Serra pron (Honda's 40941/245-5000 GONE); Ford of Columbia 1350 Nashville/(931)388-2463 (old GONE); Hyundai +address 1370 Nashville Hwy Columbia TN (no phone specified → not invented); Serra Nissan + Serra pron (addr intact); Serra Honda + Serra pron. **Vapi EVAL (chat test-suite, no billable phone call): Tony Serra Ford PASS + Ford of Columbia PASS** — transcripts show each answers the corrected address+phone live. Hyundai chat-eval flaky (tester bot ended before answer twice) → address verified via prompt read-back, not transcript. Nissan/Honda = pronunciation-only (TTS, not chat-eval-able) → read-back verified. HOLD Vapi-reads-wiki-via-MCP (later phase). Reusable eval suites left in Vapi account ("C7 eval — <store>").
- **LIVE E2E — per-store widget grounding PASS (headed Playwright, fresh browser, deployed studio):** serra-nissan public widget `/w/serra-nissan-sales-chat` — asked "do you sell Hondas?" → Caroline answered "This is Serra Nissan, and we specialize in Nissan vehicles…" (grounded on the corrected brand, does NOT claim Honda). Proves A4 live end-to-end + grounding functional. (D8/D9 UI E2E remains deploy-gated — studio serves the old build until PR #85 ships.)
- **STILL PENDING (gated / needs operator):** A2 (destructive DB purge — GO or deploy-restart window), B6 (operator picks real dealer recipient per store), C7 APPLY (Vapi write key + GO), FINAL live UI E2E for D8/D9 (deploy-gated), Vapi eval (needs key), PR #85 merge + deploy (operator GO). Fresh independent reviewer already VERIFIED all shipped code + live data.

**Method:** code audit (src/routes/api/files.ts cross-tenant read) + carried-forward dashboard/comms/sentinel audit entries.

**Method:** code audit (src/routes/api/files.ts cross-tenant read) + carried-forward dashboard/comms/sentinel audit entries.

Per standards at `~/Claude-store/sysadmin/governance-framework/file-standards.md`:
- One issue per entry. Four fields: Background, Outcome, Acceptance, Status.
- Statuses: OPEN / IN BACKLOG / NEEDS INPUT / CLOSED.
- Issues leave the file when CLOSED and shipped (moved to session-output.md for the session that shipped the fix).

### Phase 2 adversarial-check debt — logged findings (2026-07-08)

- **Background:** The Phase 2 adversarial red-team (`work/2026-07-08-phase2-adversarial-findings.md`) fixed every customer-impacting HIGH on the manual-blast path (tz-guard, prelaunch-lock E.164, persona send-guard vs prompt-injection, invalid-phone drop, detector hardening). The items below are latent / off the serra-honda manual-blast path and are carried as debt.
- **Outcome (each to be fixed before the relevant broader go-live):**
  1. **[P1] `src/routes/api/messaging/inbound.ts` generic inbound endpoint has NO STOP capture / blacklist / handle canonicalization.** Not the serra-honda SMS path (TextMagic webhook handles STOP), but any adapter posting `channel:sms` here would miss opt-out + auto-reply after STOP. Fix: extract the STOP/START + `canonicalizeContactHandle` logic from `textmagic.$profile.ts` into a shared helper and call it here before `maybeAutonomousReply`.
  2. **[HIGH, conditional] catch-up/automation sends pass no `contactId`** → with `comms.sms_consent_check` ON every send fail-closed-blocks; and VIN DNC is not consulted for SMS when the consent gate is off. serra-honda has both OFF (opt-out via carrier STOP → blacklist). Thread `contactId` (the candidates already carry it) through `sendAutomationNow`/`gatedSend` so the consent gate can run.
  3. **[MED] `comms-blacklist.norm()` doesn't +1 a bare 10-digit** → not format-invariant. Latent (producers canonicalize first). Mirror the `toE164` normalization used in `prelaunch-lock` into `comms-blacklist`.
  4. **[MED] `send-windows.nextOpenMs` is not DST-exact** (display/logging only; live send re-checks `windowOpen`). Compute the true instant before any scheduler consumes it.
  5. **[MED] operator `comms.send_windows` override has no quiet-hours validation** — add a zod `.refine` rejecting any window overlapping 21:00–08:00 local.
  6. **[MED] phantom-inbound guard bypass** when `SMS_FROM` unknown AND the echo carries no delivery-status field → phantom thread + possible self-reply. serra-honda has `SMS_FROM` set. Fail-closed: refuse auto-reply to a phone inbound when our own number can't be resolved, or drop when sender==receiver.
  7. **[MED] `toE164` primitive** mints `+<digits>` for extensions/leading-zeros/7-digit. Catch-up already drops invalid via `isValidSmsE164`; harden the primitive for other callers.
  8. **[LOW] persona send-guard** in `agent-autonomous-reply` blocks only the pricing class pre-send; inventory/specs remain alert-only (Sentinel, post-send). Extend if desired.
- **Acceptance:** each item fixed with a test before the go-live stage that exercises it (continuous driver / autonomous_reply / other-adapter inbound / other-store SMS).
- **Status:** PARTIALLY RESOLVED — **PR #70 merged + deployed 2026-07-08** shipped: item 1 (inbound `/api/messaging/inbound` STOP capture via shared `applyOptOutKeyword`, wired to webhook + inbound), item 3 (comms-blacklist norm → canonical E.164), item 7 (`isValidSmsE164` strict US/NANP). **Also fixed a P0 not previously logged:** `flattenContact` didn't unwrap the live `vin_get_contact` ARRAY shape → catch-up resolved 0 recipients (was patched in a copied file for the launch; PR #70 lands it in the tree). STILL OPEN: item 2 (thread contactId for SMS-consent gate), 4 (nextOpenMs DST — display only), 5 (send_windows override quiet-hours validation), 6 (phantom-guard fail-closed when SMS_FROM unknown), 8 (persona send-guard inventory/specs). None blocks the live campaign.

### Sentinel — no enqueue timestamp on agent_reply_jobs → can't age-detect a SINGLE stuck 'queued' job (debt, 2026-07-08)

- **Background:** `conversationOpsCheck` flags a reply-job backlog only at `queued >= 20`; a SINGLE job stuck in `queued` (the Durran-class silent-stall signature) is not caught. An age-based check ("any queued job older than N min") is not implementable: `agent_reply_jobs` (messaging-hub-store.ts schema ~231) has `attempted_at`/`sent_at` but **no enqueued/created timestamp**, so a queued job's age is unknown. The never-dead fallback (PR#68) makes a lingering `queued` unlikely (a throw now lands 'failed', which IS alerted), so this is a monitoring gap, not an active hole.
- **Outcome:** Add an `enqueued_at` column to `agent_reply_jobs` (stamped by `enqueueAgentReplyJob`) + a `Sentinel` check that warns on any queued job older than ~15m.
- **Acceptance:** A job left queued >15m raises a warning; normal fast-draining jobs do not.
- **Status:** OPEN (debt; deferred — schema migration). Persona-compliance monitoring (the D5 focus) shipped instead: `personaComplianceCheck` scans recent outbound agent SMS for pricing/inventory/specs hard-rule violations.

### Config drift — host `~/.hermes` studio.yaml ≠ live container config (truth note, 2026-07-08)

- **Background:** During Phase 2 grounding the host copy `~/.hermes/profiles/serra-honda/studio.yaml` was found to have **no `comms:` block** (so it would fall back to schema defaults: `vin_check:true`, `business_hours America/New_York 08:00–21:00`). The **live container** (`hermes-studio-…-194118185938`) `studio.yaml` DOES carry `comms.business_hours: America/Chicago 08:00–21:00` + `comms.vin_check:false`, and `.env` has `SMS_FROM=+18339785374`. Live config was hand-edited in the container volume and never mirrored to the host working tree. The host tree is stale/dev.
- **Impact:** Any claim about "current live config" read from the host is WRONG. All live-state verification for serra-honda comms MUST read the container (`docker exec … cat /root/.hermes/profiles/serra-honda/{studio.yaml,.env}`), not the host. No functional prod impact — the container is correct — but a source-of-truth hazard for this and future sessions.
- **Outcome:** Treat the container volume as the source of truth for live comms config; when Phase 2 ships config changes, apply them to the container (and, ideally, reconcile the host tree so they stop diverging).
- **Acceptance:** Live-state assertions cite container reads; host/container comms config reconciled or the divergence explicitly documented at cutover.
- **Status:** OPEN (truth note; carried into Phase 2). Pairs with the prelaunch-lock prerequisite below (issues.md "Comms pipeline driver is not running").

### P0 (SMS go-live blocker) — comms-gate vin_check fails closed on every SMS: broker `vin_query_leads` no longer supports phone-filtered DNC lookups

- **Background:** During the Serra Honda SMS go-live re-inspection (2026-07-02), the comms-gate layer 6 VIN DNC check was exercised live against the current central-mcp broker. `src/server/comms-gate.ts:140` (`vinDnc`) calls `callCentralMcpTool('vin_query_leads', { orgId, phone: to })` with no date range. The broker now **requires** `startDate`/`endDate` (zod `invalid_type` "Required" on both) → the tool errors → `callCentralMcpTool` returns `{ok:false}` → `vinDnc` returns `{errored:true}`. With `comms.vin_check` defaulting to `true` and `vin_check_fail_open` defaulting to `false` (serra-honda overrides neither), layer 6 returns `{ok:false, rule:'vin-check-unavailable'}` — **blocking every regulated (sms/voice) send for every recipient.** Additionally, when `startDate`/`endDate` ARE supplied, the broker **ignores the `phone` parameter** and returns the full lead set (`totalItems: 12784`, paged 50/page) — so a per-recipient phone-keyed DNC lookup is no longer possible through this tool at all. Evidence captured in-container via raw MCP call (matches `central-mcp.ts` transport exactly): `{orgId,phone}` → startDate-required error; `{orgId,phone,startDate,endDate}` → 12784 unfiltered leads; `tm_get_balance{account:serra_honda}` → $54.53 (broker otherwise healthy). This is a broker-side regression relative to the prior session, where `vin_query_leads`-by-phone returned 0 clean rows.
- **Outcome:** The pipeline can send within the legal window without a fail-closed VIN outage, consistent with the operator's standing decision to rely on the opt-OUT model (TextMagic carrier-level STOP → local blacklist) rather than VIN SmsPreferences ("dont worry about the opt out check"). Candidate resolutions (operator to choose): (A) set `comms.vin_check: false` for serra-honda — config-only, no deploy, removes the broken/non-functional layer; opt-out protection remains via carrier STOP + blacklist + business-hours + rate-limit + kill-switch/prelaunch. (B) set `comms.vin_check_fail_open: true` — keeps the layer "on" but sends on error; equivalent to off in effect since the tool errors every call, plus wasted broker round-trips. (C) code fix: pass a date range AND filter client-side by phone — NOT viable as-is because the broker ignores `phone`; would require a broker-side fix to restore phone filtering (cross-project, central-mcp) before the studio code could rely on it. Longer-term (separate): restore a real per-recipient VIN DNC path or replace with a first-party consent/DNC store.
- **Acceptance:** With the chosen resolution applied, a controlled live send to a non-VIN recipient (Durran, +17313946907) passes checkCommGate and dispatches from the Serra Honda number; no `vin-check-unavailable` block; the remaining gate layers still enforce (blacklist/business-hours/rate-limit verified present). Debt (B/C path + broker phone-filter loss) tracked for follow-up.
- **Status:** RESOLVED (config, 2026-07-02) via **Option A** — set `comms.vin_check: false` for serra-honda (backup `studio.yaml.bak-vincheck-20260702-185249`), matching the operator's standing opt-out decision. Verified: `checkCommGate({profile:'serra-honda',channel:'sms',to:'+17313946907'})` → `{ok:true}` (was fail-closed). Live send confirmed the path (Durran gate below). **Residual debt still OPEN (follow-up):** (1) the broker `vin_query_leads` no longer supports phone-keyed DNC lookups at all — a real per-recipient VIN DNC path must be restored broker-side (central-mcp) or replaced with a first-party consent/DNC store before `vin_check` can be meaningfully re-enabled; (2) opt-out protection now rests on TextMagic carrier STOP → `comms_blacklist` + business-hours + rate-limit + kill-switch/prelaunch; (3) other stores that rely on `vin_check` would be fail-closed by the same broker regression — audit before any other store's SMS go-live.

### P0 (go-live blocker, Part 2) — TextMagic delivery-report callbacks are recorded as PHANTOM inbounds (sender = our own number)

- **Background (found 2026-07-05 while testing serra-service OUTBOUND plumbing, at operator's prompting):** TextMagic posts a delivery-report callback for each OUTBOUND to the SAME webhook URL as inbound SMS. The payload carries `sender:<our number>`, `receiver:<customer>`, `text:<message body>`, plus a delivery `status`. The handler (`src/routes/api/webhooks/textmagic.$profile.ts`) only skips non-inbound when `!sender || !text` — but a delivery report HAS both — so it records the outbound as an INBOUND message on a thread keyed to OUR OWN number. Evidence: serra-service outbound test → phantom inbound `handle:+18553955571` `metadata:{via:textmagic-webhook, receiver:14126546500, external_id:1443003519}` with the outbound body as content. serra-honda has **7** such phantoms on `handle:+18339785374` (from A3 sends). SYSTEMIC, all profiles.
- **Impact at real go-live:** (1) Teambox pollution — a phantom "conversation" per one of our numbers; (2) spurious new-lead dealer notification when that phantom thread is first `created`; (3) LATENT self-reply loop — if any agent subscription ever lands on the our-number thread, the agent replies to our own outbounds. Does NOT corrupt real customer threads (phantom is keyed by sender=our number, separate from the customer handle), so it does not block the Durran redo (Part C).
- **Outcome:** the webhook drops any payload whose `sender` matches one of our own configured/account numbers, OR that carries a delivery-status field (`status`/`messageStatus`/`deliveryStatus`), BEFORE recording an inbound. (Alternative/complement: point TextMagic delivery-reports at a different URL in the dashboard so they never hit the inbound path.) Clean up existing phantom threads (serra-honda `+18339785374` ×7, serra-service `+18553955571`).
- **Acceptance:** an outbound send produces NO inbound record on our-number threads; real customer inbounds still record + correlate; regression test: a delivery-report-shaped payload (sender=our number + status) is ignored with 200.
- **Status:** OPEN — studio code fix + deploy, before Part 2 (real campaign go-live). Not a Part C blocker.

### Sender-pinning + service go-live config — RESOLVED via studio-side SMS_FROM pin (2026-07-05)

- **Sender-pinning (was the A3 gate):** central-mcp `tm_send_message account=serra_honda` sent from the account DEFAULT (809-6836), not the canon 833-978-5374, even with `sms_number` in config. RESOLVED studio-side (no central-mcp change) by setting **`SMS_FROM=+18339785374`** in `~/.hermes/profiles/serra-honda/.env` → `dispatchSms` passes it as the TextMagic `from`. VERIFIED: A3 round-trip all delivered from 978-5374 (send + Caroline auto-reply, status `d`). Same pin applied for service: **`SMS_FROM=+18553955571`** in serra-service `.env`; service outbound test delivered from 855-395-5571. This also PROVED both accounts own/can send-from their canon numbers. Operator may move pinning into central-mcp later and revert these `.env` pins.
- **serra-service go-live config (set 2026-07-05, mirrors Honda):** `comms.vin_check:false` (backup `studio.yaml.bak-svctest-*`) + `SMS_FROM`. **GAP still OPEN:** serra-service has NO `comms.business_hours` → defaults to America/New_York; must be set to **America/Chicago 08:00–21:00** before service go-live (Honda already fixed). The service outbound test used a per-call business-hours bypass (off-hours 23:15 CT) — not a config change.
- **Status:** sender-pinning RESOLVED (both lines). serra-service business_hours = OPEN (pre-go-live).

### P0 (serra-service go-live blocker) — 855-395-5571 sends are "delivered" in TextMagic but NEVER reach the handset (toll-free not verified)

- **Background (2026-07-05):** serra-service outbound test to the owner (+14126546500) reported `status:d` / `deliveredAt:2026-07-05T04:16:28` / price 0.049 in TextMagic (`tm_get_message` id 1443003519, sender 18553955571) — but the owner **did not receive it on the handset**. By contrast Honda's 833-978-5374 (also toll-free) delivered reliably end-to-end in A3 (owner received + replied). Signature of an **unverified/unregistered US toll-free number**: carriers accept the send (TextMagic marks "delivered") but drop it before the handset until TFN verification/registration is complete.
- **Outcome:** 855-395-5571 passes toll-free verification/registration in TextMagic so real handsets receive service sends. (Owner/TextMagic-admin action — not visible via broker tools.)
- **Acceptance:** a service send to a real handset is actually received (not just `status:d`), confirmed by the recipient.
- **Status:** OPEN — owner/TextMagic-admin. Blocks serra-service go-live only; does NOT affect Honda/Durran. The service INBOUND callback works (Connection-passed test landed); the OUTBOUND delivery is the gap.

### CORRECTED — TextMagic inbound callback DOES deliver (my earlier "not wired" diagnosis was WRONG); real blocker is handle normalization (see next entry)

- **Correction (truth-over-compliance, 2026-07-02):** I earlier reported that Durran's reply "never reached the studio / callback not wired." That was WRONG. Durran's "Absolutely!!" WAS delivered to the studio webhook and recorded (thread `2a745ea0`, `metadata.via: textmagic-webhook`, created 19:09:49Z & 19:11:29Z), and a TextMagic callback test ("Test message status" from 999321654) was delivered 21:56Z. My error: I only queried the go-live thread `524cb2cc` (which had 0 inbound) and misread the TextMagic "unread" flag (that flag only means unread-in-TM-UI, not undelivered) + the app doesn't log webhook POSTs. Inbound delivery is CONFIRMED WORKING for serra-honda.
- **Why Caroline still didn't reply:** the inbound landed on a DIFFERENT thread than the outbound/subscription thread, because of a phone-handle format mismatch — see the handle-normalization P0 below. Not a callback problem.
- **Residual TODO:** verify serra-service inbound callback the same way (real reply → studio); triage/clean the TextMagic test artifact ("Test message status") + the 528-unread lane; consider setting `TEXTMAGIC_WEBHOOK_SECRET` (endpoint is currently unauthenticated).
- **Status:** CALLBACK CONFIRMED WORKING (serra-honda). serra-service pending real-reply verification.

### P0 (SMS go-live blocker) — inbound webhook does NOT normalize the sender to E.164 (+), so replies split onto a new thread away from the agent subscription

- **Background:** `src/routes/api/webhooks/textmagic.$profile.ts` (~line 166) creates/matches the thread with `contact_handle: sender` **raw**. TextMagic posts the sender WITHOUT a leading `+` (e.g. `17313946907`), while outbound producers (vin-watcher, automations, lead-flow) store E.164 WITH `+` (e.g. `+17313946907`). `getOrCreateThread` keys on the exact `contact_handle` string, so inbound and outbound never correlate. Consequences, all confirmed on serra-honda: (1) the reply lands on a NEW thread (`2a745ea0`) separate from the agent's outbound thread (`524cb2cc`), so the `mode:reply` subscription is absent on the inbound thread → **the agent never auto-replies** (this, not the callback, is why Caroline was silent to Durran); (2) because the inbound thread is `created=true`, the handler fires a **new-lead dealer notification on every reply** → BDC email spam per customer message; (3) the conversation is split across two Teambox threads. Blast radius already visible: 6 serra-honda SMS threads, **2 phone numbers split across +/no-+ threads**. This breaks the conversational flow for EVERY lead at go-live, not just Durran.
- **Outcome:** The webhook normalizes `sender` to canonical E.164 (`+` + digits, prepend `+` when missing) BEFORE `upsertContact`/`getOrCreateThreadEx` (ideally a single shared `normalizeHandle` used by both inbound and outbound so they always agree). After the fix, a reply reuses the existing outbound thread, the agent's `mode:reply` subscription fires, and no duplicate new-lead notification is sent. Small studio code change; needs deploy to the running container. Pre-existing split threads (Durran) reconciled/cleaned.
- **Acceptance:** A real reply from a test handset to 833-978-5374 lands on the SAME thread as the outbound, the agent auto-replies, and NO second new-lead notification fires. Regression test: webhook sender `17313946907` matches an outbound thread stored as `+17313946907`.
- **Status:** OPEN — studio code fix + deploy. Top blocker for the "successful conversation" half of the go-live gate.

### P0 (SMS go-live blocker) — serra-honda outbound sends from the account DEFAULT number (833-809-6836), not the canon serra_honda number (833-978-5374)

- **CANON (operator, 2026-07-02):** only two numbers are provisioned — **833-978-5374 = serra_honda (sales, Caroline)** and **855-395-5571 = serra_service (service)**. Numbers 833-893-5694 and 833-809-6836 are NOT ours and must not be used. The stale `docs/launch/TEXTMAGIC_WIRING.md` (833-893-5694 as Honda; 833-978-5374 as service) is SUPERSEDED by this canon.
- **Correction to my own prior entry (truth-over-compliance):** I earlier called central-mcp `textmagic.accounts` "mislabeled." That was WRONG — it already matches canon: `serra_honda.sms_number:"+18339785374"`, `serra_service.sms_number:"+18553955571"`. I had compared it against the stale doc.
- **Background (the real defect):** The Durran sends left from TextMagic **833-809-6836** (proven: `tm_get_chat_by_phone` chat 43595689 `to:18338096836`), NOT from the canon 833-978-5374. Cause: the studio passes NO `from` (documented in `docs/launch/NEXXUS_FIT_PLAN.md:75` — "greeting sends pass NO `from` (MCP default sender). Dev default `18338096836`"), so central-mcp's `tm_send_message account=serra_honda` uses the credential's TextMagic **default sender = 809-6836** instead of the configured `sms_number` 833-978-5374. So config having the right number is necessary but NOT sufficient — the broker must actually SEND FROM it. Also corrected: the earlier `work/2026-07-01-serra-sms-golive-proof.md` claim "Sends from the Honda number ✅ VERIFIED-LIVE" was never verified on the wire and is WRONG. The account also shows **528 unread** on the 809-6836 lane (possibly-unmonitored real inbound).
- **Outcome:** central-mcp pins the `serra_honda` sender to **833-978-5374** (via `from` param or by setting the account default) so sends never fall back to 809-6836; confirms 833-978-5374 is owned/sendable by the `serra_honda` credential; and sets that number's inbound callback → `/api/webhooks/textmagic/serra-honda`. STUDIO SIDE IS ALREADY CANON-CLEAN (forces no number, hardcodes none, relies on the account) — no studio change needed for the sender. Operator is applying the central-mcp fix.
- **Acceptance:** A test send from serra-honda leaves from **833-978-5374** (verified via `tm_get_chat_by_phone`/handset), and an inbound reply to that number round-trips to the studio (see inbound-callback P0). 528-unread backlog triaged before go-live.
- **Status:** OPEN — operator owns the central-mcp/TextMagic-admin fix (in progress 2026-07-02). Studio side verified clean. Pairs with the inbound-callback P0. Re-verify with a real round-trip after the fix.

### P0 — /api/files GET cross-tenant file read (no scope check)

- **Background:** `src/routes/api/files.ts` GET (lines ~263–334) gates only on `isAuthenticated`, then reads `profile` from the query string and serves that profile's workspace files (list / read / download) via `getEffectiveRoot(profileName)` with **no** `isAuthorizedForProfile` check. Contrast the POST handler at ~line 340, which requires `isAdmin`. Any authenticated tenant — a single-store workspace customer-admin or a scoped partner — can pass `?profile=<another store>` and read another tenant's workspace files. Found by direct code read during backend-cert grounding (2026-06-27); not yet proven live.
- **Outcome:** The GET handler enforces `isAuthorizedForProfile(resolveSession(request), profileName)` and returns 403 before `getEffectiveRoot` when the session is not authorized for the requested profile. Super-admin retains full access; scoped sessions are limited to their own/authorized profiles.
- **Acceptance:** Leak proven live with a scoped session before the fix; after the fix the same request returns 403 while an admin request still succeeds; a regression test covers denied-cross-profile + allowed-admin; full suite + build green.
- **Status:** FIXED IN BRANCH (`chore/backend-cert-portability`, uncommitted) 2026-06-27. Leak PROVEN deterministically: `src/test/files-api-tenant-isolation.test.ts` failed 3/5 against the original handler (scoped partner read another tenant's file = 200 "HONDA TENANT SECRET"; single-store customer-admin cross-store = 200; non-admin read of `profiles/serra-honda/auth.yaml` password hash via global root = 200). PATCH: `src/routes/api/files.ts` GET now requires `isAuthorizedForProfile(resolveSession(request), profile)` for a profile-scoped read and `isAdmin` for the global root, before `getEffectiveRoot`. **2026-06-29 — independent code review found the first patch INCOMPLETE:** the `action=list` GLOB branch called `readGlobDirectory` against the global `WORKSPACE_ROOT`, so a scoped session that passed the auth check could still enumerate the global `~/.hermes` root / other tenants' directory structure (names/sizes/mtimes, not contents). FIXED: `readGlobDirectory` now takes the `effectiveRoot` (scoped) + 2 regression tests added (glob scoped to own root; traversal toward another tenant returns nothing). Orphaned legacy `ensureWorkspacePath` removed. Post-fix all 7 isolation tests pass; full suite 1024 passed/1 skipped; build clean. REMAINING: commit + open PR; merge + Coolify deploy on explicit GO; then live cross-tenant denial verification on studio.huminic.app. Note: the operator-only `/files` + `/profiles` nav surfaces may be reachable by scoped partners — UI hardening follow-up (the API now denies correctly regardless).

### Model-selector label shows the profile name (e.g. "serra-honda") not the model + on-login 404 noise

- **Background:** The composer's model chip showed the active-profile-at-boot name instead of the real model. Verified ground truth (2026-06-27, in-container): the `hermes-agent` gateway has **no `/api/config`** (404), and `/v1/models` returns a single entry `{id:"serra-honda", owned_by:"hermes"}` — the frozen boot-profile name. The Studio's `/api/models` passed that through, so `configuredModel` (the label fallback) read "serra-honda". Separately, `chat-composer.tsx` called `/api/hermes-proxy/api/available-models` on every login, which 404s on stock Hermes → a red console error each login. Confirmed the real chat send (`use-streaming-message.ts`) posts **no `model` field**, so routing is per-profile/session-based and the label is purely cosmetic — relabeling is routing-safe.
- **Outcome:** `/api/models` surfaces the real configured model (`gpt-4.1` / `custom` from `~/.hermes/config.yaml` via new `getConfiguredDefaultModel()`) as the primary entry (gateway profile-name entry kept but no longer the label); `chat-composer.tsx` drops the legacy `available-models` + direct-gateway calls and uses `/api/models` directly (kills the on-login 404). Optional root-level fix (GATED): set `API_SERVER_MODEL_NAME=gpt-4.1` on the `hermes-agent` Coolify service so `/v1/models` itself advertises `gpt-4.1` (brief gateway/inference restart for all accounts).
- **Acceptance:** Composer label reads `gpt-4.1`; no `available-models` 404 in the console on login; chat still streams. Verified live headed on studio.huminic.app after deploy.
- **Status:** FIXED IN BRANCH (`chore/backend-cert-portability`, uncommitted) 2026-06-27 — Option B (Studio-only, no gateway restart). Tests: `src/test/models-api-label.test.ts` (3) + full suite 1022 passed/1 skipped + build clean. REMAINING: commit + PR; merge + deploy on GO; then live headed verification. Option A (gateway env) pending explicit GO (gateway redeploy).

### brain/{assumptions,readiness,uploads} use an inline scope check missing the partner `scope_profiles` branch (functional gap, NOT a leak)

- **Background:** During the Phase-3 Tranche-1 auth sweep (2026-06-27, independent agent audit + main-agent spot-check), three brain routes (`src/routes/api/brain/assumptions.ts:64-66`, `brain/readiness.ts:59-61`, `brain/uploads.ts:47-49`) gate with an inline `session.is_admin || (session.is_customer_admin && session.profile === profile)` instead of the shared `isAuthorizedForProfile`. This OMITS the `scope_profiles` branch, so a scoped partner admin (e.g. Durran/cage-automotive) is wrongly **denied (403)** brain assumptions/readiness/uploads for a profile that is in their scope. The fail-direction is safe (no cross-tenant leak — it denies rather than over-grants); this is the opposite of the files.ts bug.
- **Outcome:** Replace the inline check with `isAuthorizedForProfile(resolveSession(request), profile)` for consistency, so scoped partners get the access they are entitled to. NOTE: this is a behavior change (broadens partner access to partner-scoped brain data) — confirm with operator before shipping, given the explicit "do not change partner scoping without familiarizing first" instruction.
- **Acceptance:** A scoped partner can read brain assumptions/readiness/uploads for an in-scope profile; out-of-scope still 403; super-admin unaffected.
- **Status:** OPEN — cert-debt (functional, safe direction). Surface options + get GO before changing partner access. Not in the P0/Phase-1 PRs.

### Auth & tenant-isolation sweep — CERTIFICATION EVIDENCE (Tranche 1/2)

- **Background:** Phase-3 cert (2026-06-27) audited all 53 profile-scoped API routes for the files.ts vulnerability class (profile data served on bare `isAuthenticated`). Method: independent general-purpose agent classification + main-agent spot-check re-verification.
- **Outcome (evidence):** 0 VULN, 0 NEEDS_REVIEW. 38 OK_SCOPED (`isAuthorizedForProfile`), 9 OK_ADMIN_ONLY (`isAdmin`: agent-migrations, artifacts, knowledge/{graph,list,read,search}, workspace), 4 OK_PUBLIC (public/* widget endpoints, profile bound to widget/config), 2 non-profile-scoped. `messaging/inbound.ts` uses a per-profile `HERMES_INBOUND_TOKEN` (profile-bound). `studio-config.ts` returns full config only to authorized sessions, a stripped public subset otherwise. `files.ts` was the lone outlier and is fixed. Spot-checked: brain/assumptions (inline check), performance (isAuthorizedForProfile), knowledge/read (isAdmin) — all confirmed.
- **Acceptance:** Verdict folds into `work/<date>-backend-certification-report.md`; live cross-tenant denial proof on the deployed files.ts fix.
- **Status:** EVIDENCE CAPTURED — Tranches 1 (auth boundaries) + 2 (data scoping) substantially PASS at the code level; live proof pending the files.ts deploy.

### PROD INCIDENT — erroneous weekly "AI Dealership Performance Analysis" emails sent to customers

- **Background:** On 2026-06-22 07:02 CDT, real customers (Serra Nissan, Tony Serra Ford, Serra Honda org_admins incl. `dwood@serrahonda.net`, `victoria@misscommunicationconsulting.com`, cc Cage partner `durran@cageautomotive.com`) received automated, all-zeros, "DRAFT - TRAINING IN PROGRESS" weekly reports that were supposed to be disabled. **Proven root cause** (container logs — see `work/2026-06-22-weekly-report-incident.md` RESOLUTION): a leftover **Coolify deployment on this server** — app id 2 `nexxus-production` (uuid `phqqzjj5pal13wlp39m5ohx6`, container up since 2026-05-14) — runs an old pre-May-2 Nexxus build whose weekly scheduler is the UNCONDITIONAL (ungated) version. It fired every Monday 7:00 CT via Resend `leads@huminic.ai` (proof: messageId `e045adb2-…` = the delivered Tony Serra Ford email). NOT Replit (release-1r was UI-only); NOT the pm2 `nexxus-app` (new build, scheduler disabled). Prior fixes disabled the pm2 backend + host cron but never this Coolify container — hence the recurrence ("third time").
- **Outcome:** The leftover Coolify container is stopped so no further automated reports send; affected customers receive a correction; reporting resumes only from a verified path.
- **Acceptance:** Coolify `nexxus-production` stopped (no auto-restart/redeploy); no Resend send Mon 2026-06-29; customer apology sent to all affected recipients.
- **Status:** ✅ SENDER DISABLED 2026-06-22 (container stopped via docker + Coolify `StopApplication`; status `exited`; port 5001 closed; legit `nexxus-app` :5000 unaffected). REMAINING: (a) operator delete Coolify app id 2 for permanence; (b) confirm no send Jun 29; (c) send customer apology — draft `work/2026-06-22-customer-apology-email-DRAFT.md` (not sent), expand recipient list to all affected orgs.

### Super-admin password did not match the value reported as "set + verified"

- **Background:** During the 2026-06-25 Studio UI live verification, the super-admin login (`duane.wells@huminic.ai`) on studio.huminic.app rejected the password `Hum1n1c$ucce$` that the 2026-06-24 session reported as "set + verified end-to-end." Direct `verifyPasswordHash` against the live `auth.yaml` hash returned **false** for that value (and for the obvious shell-mangled variants). Root cause (most likely): a `$`-expansion in the shell at set-time on 2026-06-24 stored a different string, and that session "verified" by comparing the mangled value against itself (a false positive), never via an actual login. This is an honest-recovery record per core value #11.
- **Outcome:** Password re-set to the operator's exact intended value `Hum1n1c$ucce$` using base64 to bypass shell `$`-expansion, and verified by a **real browser login** (not self-comparison). Old file backed up in-container at `/root/.hermes/profiles/huminic/auth.yaml.bak-20260625`.
- **Acceptance:** Operator can log in to studio.huminic.app with the intended password — CONFIRMED via headed Playwright login 2026-06-25. Future password sets must be verified by an actual login, never by comparing the just-hashed value to itself.
- **Status:** ✅ RESOLVED 2026-06-25 (password restored + login-verified). Process note carried forward: verify auth changes end-to-end.

### Studio left-nav `sidebarCollapsed` persistence — mobile-resize edge case (minor debt)

- **Background:** `src/stores/workspace-store.ts` now persists `sidebarCollapsed` (so the desktop pin position is remembered — operator list item 1). `ChatSidebar` renders desktop-only, but the mobile force-collapse effects in `workspace-shell.tsx` still write `sidebarCollapsed=true`. If a *single browser* is resized across the 768px breakpoint after pinning the sidebar open, the mobile write can persist `collapsed=true` and the desktop pin is lost on return. No cross-device impact (separate localStorage); no impact for desktop-only use.
- **Outcome:** If it ever matters, split the desktop pin (persisted) from a transient mobile-drawer flag so mobile writes never touch the persisted desktop value.
- **Acceptance:** Pin position survives a same-browser resize across the breakpoint.
- **Status:** OPEN (minor; accepted for now — operator uses desktop).

### Super-admin lands scoped to the global active profile (Serra Honda) — item 2

- **Background:** Operator list item 2. The active profile is a single **global** server-side file (`~/.hermes/active_profile`, currently `serra-honda`), last-switched-wins across all sessions — not per-user. The super-admin entry (`/` → `/chat`) inherits it, so login drops into whatever customer was last activated. Operator decision (2026-06-25): super-admin should **land unscoped / on the profile chooser**, not auto-scoped into a customer.
- **Outcome:** Operator revised the decision (2026-06-26) from "chooser" to **"land on the huminic profile."** Implemented in `login-screen.tsx`: on super-admin login (gated `is_admin === true`), activate the operator's own `huminic` profile before reloading into Global Studio. Workspace single-store admins (first branch) and scoped partners like Durran/cage-automotive (final `else` → reload) are byte-for-byte unchanged; data access stays scope-gated via `isAuthorizedForProfile`.
- **Acceptance:** Fresh super-admin login lands on `huminic` (not Serra Honda); Workspace + scoped-partner sessions unaffected; verified live.
- **Status:** ✅ RESOLVED 2026-06-26 — PR #63 merged + deployed. Verified live: fresh super-admin login flipped `~/.hermes/active_profile` `serra-honda` → `huminic`, sidebar/header show "Huminic", `/api/profiles/list` activeProfile=`huminic`. Note: the model-selector chip near the composer still shows a stale label (`serra-honda`) because `/api/hermes-proxy/api/available-models` 404s — pre-existing infra gap, unrelated to this change.

### Comms pipeline driver is not running in the Studio (Phase 0 blocker)

- **Background:** Phase 0 verification (2026-06-23, `work/2026-06-23-phase0-readiness-report.md`) found `runDueWork()` is never invoked in this environment — no host cron, no container cron, no systemd timer, `startCommsScheduler()` never called, and production `server-entry.js` does not boot it. So campaigns, lead follow-ups, vin-watcher, lead-flow, and escalations all currently never tick in the Studio. The SMS go-live (and the Sentinel) both depend on a periodic tick.
- **Outcome:** A driver runs `runDueWork()` on a ~1-min cadence (reuse existing pattern: host cron `docker exec … npx tsx scripts/comms-cron.ts`, or boot `startCommsScheduler()` in `server-entry.js`), with the prelaunch lock engaged first so wiring it cannot leak real sends.
- **Acceptance:** A heartbeat (`sentinel_meta.comms_heartbeat`, now stamped by `runDueWork`) updates every pass; the Sentinel comms-liveness check goes green.
- **Status:** OPEN — Phase 3 prerequisite. Wiring is infra (coordinate via sysadmin). Safety: `OUTBOUND_LIVE_ENABLED=true` and `PRELAUNCH_SMS_LOCK` absent → engage the lock with the operator's number BEFORE wiring any driver.

### Sentinel monitor — remaining work after the core build (debt)

- **Background:** Phase 1 (2026-06-23) built the Sentinel core: `src/server/sentinel.ts` (engine + dedup/throttle + severity email alerts + daily all-clear heartbeat + auto-resolve) with four checks — comms-cron liveness, agent/thread SLA, integration reachability (real probes: `vapi_list_assistants`, `tavus_list_personas`, `vin_token_status`), and app-health (surfaces `runDueWork` per-pass error count); `scripts/sentinel-cron.ts`; heartbeat+error signal wired into `runDueWork` (`src/server/comms-heartbeat.ts`). 13 unit tests (induced failure per category); full suite 969 + build green.
- **Outcome / remaining:**
  - **Credit-balance alerting is NOT possible via central-mcp** — confirmed via tools/list: there is no Vapi/Tavus/TextMagic balance tool and no Resend health endpoint. Monitoring credit balances would need provider-direct API access (separate keys; likely sysadmin). Today the Sentinel covers reachability/auth (provider down/unauthed) + SMS/email *delivery* failures via app-health, NOT remaining-credit thresholds.
  - **Raw cross-service log/exception scraping** still needs a log source the app does not expose (only the comms-pass error count is surfaced today).
  - **In-app alert channel:** findings are recorded to the Brain `sentinel_event` table but not yet surfaced via a read endpoint/UI (email channel works).
  - **Thread-SLA data-path test** with seeded threads (engine path tested; the store path is not).
  - **Wire the `sentinel-cron` tick** so it runs live (infra → sysadmin) and do the live induced-failure verification in prod.
- **Acceptance:** balance limitation documented/decided; in-app feed visible; Sentinel running live and verified against an induced failure per category in prod.
- **Status:** OPEN (debt; core shipped on branch `feat/sms-golive-sentinel`, uncommitted). Tick-wiring is the gating step to make it live; balance monitoring is a scope decision (provider-direct access).

### Dashboard funnel reads ~1.7–2x the CRM

- **Background:** `buildFunnelTab` builds the funnel entirely from the latest uploaded ROI report and uses the raw `total_leads` column (includes BAD + DUPLICATE) for the Leads stage; service + sales are blended; timing is pulled from a source that does not mean what the dashboard claims. Customer reconciliation: leads 660 vs 388, appts set 120 vs 67, sold 58 vs ~30, time-to-appt 25.6d vs 5.7, time-to-sale 33.6d vs 3.8.
- **Outcome:** Every funnel number is either a defensible value or the literal "needs supplemental data" — never inflated. Lead counts come from the API (sales-scoped, BAD dropped, deduped by contact) and reconcile to the CRM at lead-source granularity within a documented tolerance.
- **Acceptance:** See plan.md Definition of Done. Reconciliation test passes against a real CSV; dashboard reads the canonical Brain funnel; live headed sweep shows only defensible numbers or the fallback text.
- **Status:** DEPLOYED + LIVE-VERIFIED on real data (merged to main, Coolify app 3). hyundai raw 2741 → **1545 defensible** (~1.77x reduction, matching the complaint); all VIN stores show sourced API Leads (hyundai 1546, ford 710, serra-honda 282, serra-nissan 228, tony-serra-ford 168), report-only metrics render "needs supplemental data" where no report, non-VIN stores degrade to pending. Two real-data fixes applied during verification: (1) lead-source URLs → names via `vin_get_lead_sources`; (2) lead-type scope corrected to all retail sales types (REFERRAL/PREVIOUS_CUSTOMER/WEBSITE_CHAT/IMPORT incl., service/parts/wholesale excl.) after the undercount guard flagged REFERRAL at tony-serra-ford. Remaining: final live sweep on the lead-type redeploy + independent review.

### Reconciliation CSV not yet provided

- **Background:** Certifying the Leads count as defensible requires the real CSV (known store + known 30-day window) the customer's 388/67/30/5.7/3.8 numbers came from, placed inside the workspace (file-boundary hook blocks reading ../nexxus). Operator is retrieving it.
- **Outcome:** CSV placed in the workspace so Phase 4 reconciliation can run.
- **Acceptance:** A CSV is present and the reconciliation test asserts API per-source counts vs it within tolerance.
- **Status:** NEEDS INPUT (operator). Reconciliation harness is WIRED and waiting — `src/test/dashboard-reconciliation.test.ts` (skipped until both `src/test/fixtures/reconciliation/report.csv` + `api-leads.json` exist; see that README). Asserts the hard invariant (per-source deduped ≤ raw Total_Leads) and prints per-source deltas for the write-up.

### vin_query_leads fetched single-page only

- **Background:** `buildLeadFunnel` (customer-reports.ts) fetches only the first page (pageSize 50) of `vin_query_leads` and trusts `totalItems` for the count; `by_status`/dedupe see only the first 50 rows. Proper dedupe-by-contact + sales scoping across a window requires paginating all pages.
- **Outcome:** The new API lead engine paginates every page before classifying/deduping.
- **Acceptance:** Engine pulls every page (stops on the reported total or a short page; caps at MAX_PAGES and surfaces `capped`) and dedupe operates over the full window; covered by unit tests.
- **Status:** FIXED IN BRANCH (`fetchAllLeads` in lead-opportunities.ts; tested incl. absent-totalItems + capped). Pending merge.

### Report metrics gated OFF — uploaded reports populate nothing in the funnel middle (debt)

- **Background:** Live verification (serra-honda, 2026-06-22) showed the uploaded VinSolutions report reads ~1.8–2x the CRM for the selected window (report sold 58 / appts 120 vs CRM ~30 / 67; API sold 30 matches CRM). Root cause is most likely a window mismatch (the report's period ≠ the dashboard's trailing window) and/or bad+dup inclusion. So `REPORT_METRICS_TRUSTED=false`: contacted / appointments / timing / gross render "needs supplemental data" rather than inflated numbers. Consequence: an uploaded report currently contributes nothing to the funnel middle stages.
- **Outcome:** A trustworthy, window-matched report ingestion path (the backlogged intelligent CRM-Guru ingester) that lets these metrics be displayed defensibly; then flip `REPORT_METRICS_TRUSTED`/pass `trustReport`.
- **Acceptance:** Report-derived metrics reconcile to the CRM for the selected window within a documented tolerance, then are re-enabled.
- **DECISIVE FINDING (code-verified 2026-07-10) — why "window-matched" cannot simply be built:** the ingested report carries **no real coverage window to align to.** `report-ingest.ts:280 periodFromFilename` scrapes a **single ISO date** from the filename (regex `\d{4}-\d{2}-\d{2}`) and writes it to BOTH `period_start` and `period_end` (`:380-404`) — a zero-width point, or `null` when the filename has no date. The VinSolutions ROI/KPI CSV itself has **no date-range columns** (every `ROI_COLUMNS`/`KPI_COLUMNS` entry is a metric). `dashboard-metrics.ts:257` only reads `period_start` as a sort key / comparison label, never as a coverage range. So the P1-5 blocker is **missing source data, not un-written code**: to align a report to the dashboard's trailing live-API window you need the report's true start→end, and it does not exist in the upload. Building alignment off the single filename date — or inferring a range — would fabricate the exact period boundary whose mismatch produced the original 1.7–2× inflation. The anti-inflation guard (`crm-guru.ts assembleCanonicalFunnel`, sum-across-sources ≤ live Leads×1.05) is built + Checker-verified + live SAFE, but it enforces "report ≤ live count," NOT "same period."
- **Unblock options (need operator, no autonomous path):** (A) capture the real reporting period at upload time — operator enters period start+end, OR adopt a filename/CSV convention that encodes a true range — then window-matched display is buildable + testable + safe; (B) leave display OFF (current live state: `REPORT_METRICS_TRUSTED=false`, zero inflation, honest "needs supplemental data"). Re-enable also still needs the `total_gross` no-baseline rule (line ~321) + Checker 5-store live-VIN pass.
- **Status:** OPEN (debt; operator-flagged + accepted per the goal's "defensible or honestly absent" rule). Ties to the backlogged intelligent CRM-Guru ingester. Blocked on operator decision (A vs B) — NOT autonomously completable without inventing a period boundary.

### Canonical funnel persisted on every dashboard load; read-back path unused (debt)

- **Background:** `persistCanonicalFunnel` writes a `canonical_funnel` snapshot to the Brain on every `/api/customer/dashboard` request. Growth is bounded to the most-recent CANONICAL_RETENTION (50) snapshots per profile. `loadLatestCanonicalFunnel` exists for audit/history but is not yet wired to a production read path — the dashboard currently assembles the canonical funnel fresh each request (the live API window is per-request) and returns that.
- **Outcome:** Either wire a read path that serves the persisted snapshot where a live assemble is unnecessary, or keep persist as audit-only and document it as such.
- **Acceptance:** Decision recorded; no unbounded growth (already capped); no dead code left unexplained.
- **Status:** OPEN (low-priority debt; recorded per global rule 8).

### Leads "vs prior" trend is neutral (no prior API window fetched)

- **Background:** The API Leads stage shows no period-over-period arrow because only the current window is fetched (a second prior-window API call was avoided to halve load). Report-based downstream stages still trend vs the prior report.
- **Outcome:** Optionally fetch the prior API window to populate the Leads trend, if the load cost is acceptable.
- **Acceptance:** Either implemented, or accepted as a documented limitation.
- **Status:** OPEN (low-priority; documented limitation).

### Dashboard FunnelView render error on local dev (IN BACKLOG)

- **Background:** `funnel.lead_performance.map is not a function` seen once on an initial local `/dashboard` load. Current performance-renderer correctly uses `lead_performance.stages`.
- **Outcome:** Confirm live defect vs stale-page artifact; fix if real.
- **Acceptance:** Reproduced on a clean prod-shaped load and fixed, or confirmed as a stale-page artifact.
- **Status:** IN BACKLOG (backlog.md "Observed during testing").

### Sentinel — Vapi/Tavus credit-balance monitoring not possible via central-mcp (debt)

- **Background:** The Sentinel monitors vendor reachability for Vapi, Tavus, and VIN via central-mcp probes, and TextMagic reachability **plus remaining balance** via its direct REST API (`/api/v2/user`). central-mcp exposes NO credit/balance tool for Vapi or Tavus and no Resend health endpoint (confirmed via tools/list), so remaining-credit threshold alerting for those two providers is not implementable through the current surface.
- **Outcome:** Provider-direct API access (Vapi/Tavus keys) to read balances, then add low-balance checks alongside the TextMagic one.
- **Acceptance:** Vapi + Tavus balance readable and alerting below a configurable threshold, with credentials managed per the env conventions.
- **Status:** OPEN (debt; reachability + delivery-failure coverage shipped now per operator scope decision 2026-06-23).

### Sentinel — AI conversation-quality grader needs live verification (debt)

- **Background:** `conversationQualityCheck` grades a sampled, throttled set of conversations via the app's own inference (`defaultGradeConversation` → Hermes `/v1/chat/completions`, OpenAI fallback). The check logic (sampling, hourly throttle, score≤3 ⇒ finding, fail-safe on grader error) is unit-tested with an injected grader, but the default grader's live endpoint/prompt has not yet been exercised against a real model in this environment.
- **Outcome:** Confirm the grader returns parseable scores from live inference during Phase 1 live verification; tune the rubric/threshold.
- **Acceptance:** A known-bad transcript scores low and raises a finding live; a known-good one stays clean; cost per pass is bounded by the sample size + throttle.
- **Status:** OPEN (debt; verify during live Sentinel verification).

### Sentinel monitor boot mechanism — RESOLVED (in-container supervised process)

- **Background:** Production boots via the custom `server-entry.js` → bundled `dist/server/server.js`. Resolved by having `server-entry.js` supervise `scripts/sentinel-daemon.ts` as a sibling process **inside the same container** (via `npx tsx`, the proven in-container script mechanism per the Dockerfile), env-gated by `SENTINEL_TICK_ENABLED`. Runs where the per-profile data + provider keys live; restarts on exit; independent fate from the request handler.
- **Status:** RESOLVED — deployed to Coolify app 3 (main `9a2c3da33`, deploy `pf9qla0lvu0c…`). Daemon confirmed alive; `SENTINEL_TICK_ENABLED=true`, `SENTINEL_ALERT_EMAIL=duanekwells@gmail.com`. The serra-honda comms tick (`COMMS_TICK_ENABLED`, in `background-ticks.ts`) stays OFF — separate SMS go-live work, parked.

### sendNotification sent `to` as an array — central-mcp rejects it — FIXED

- **Background:** `sendNotification` passed `to` as an array to central-mcp `resend_send_email`, which requires a STRING (`Expected string, received array`). This silently broke EVERY email through `sendNotification` (incl. all Sentinel alerts; single recipients too, since they were array-wrapped). Found by live deploy: first pass found 12 issues, sent 0 alerts. An existing test had enshrined the buggy array shape.
- **Status:** FIXED (PR #57). Recipients joined to a comma-separated string (also cc/bcc); test corrected + multi-recipient case added. Verified live: a 12-finding pass returned `alertsSent=12`, `errors=[]`.

### Sentinel suppressed alert retries after a failed send — FIXED

- **Background:** The engine stamped `last_alerted_at` during upsert regardless of send success, so a failed alert email would not retry for the full 6h re-alert window.
- **Status:** FIXED (PR #57). The alert ledger is stamped only after a confirmed send (`markAlerted`); a failed send is retried on the next pass.

### Sentinel finding-noise tuning (low-priority debt)

- **Background:** First live pass surfaced 6 "unanswered customer message past SLA" (open threads whose last inbound is >30m old — may include long-abandoned threads) and 5 "no inbound in 48h" data-collection warnings (some stores may be demo/inactive). All truthful, but potentially noisy.
- **Outcome:** Optionally scope/threshold these (e.g. only count SLA breaches where the inbound is within the last N hours; suppress data-collection for known-inactive profiles or via an allowlist of monitored stores).
- **Acceptance:** Operator confirms the alert set is actionable; thresholds tuned if not.
- **Status:** OPEN (low-priority; observe the first days of alerts before tuning).

### Sentinel synthetic widget monitor — DEPLOYED + verified (in-container Chromium)

- **What:** A daily real-browser (Playwright/Chromium) check loads each customer's PUBLIC site and verifies our embedded widget renders + opens + shows the expected channels. Broken ⇒ CRITICAL email; browser-unavailable ⇒ one "coverage down" warning.
- **Deploy note (lesson):** Coolify builds the Studio from `docker/workspace/Dockerfile` (build_pack=dockercompose), NOT the root `Dockerfile` — Chromium must be installed there. Verified live: Chromium 149 present in-container; `checkWidget("https://www.serrahonda.net")` → `ok=true` (script + launcher + chat/callback/video/form); full pass 166 checks, 0 widget findings, 0 errors.
- **Status:** RESOLVED — deployed (main `ecfff25c8`). Runs once/24h inside the Sentinel daemon.

### Widget monitor — configure remaining customers as their widgets go live (follow-up)

- **Background:** Coverage is per-customer via `SENTINEL_WIDGET_TARGETS` (app-3 env, JSON map). Only **serra-honda → https://www.serrahonda.net** is configured today (first live widget). "Standard for all customers" requires adding each dealer's public URL when their widget goes live.
- **Outcome:** As each dealer embeds the widget, add `"<profile>": {"urls":["https://<their-site>"],"expectChannels":[...]}` to the env map (or `~/.hermes/sentinel-widget-targets.json`). Until added, that store has no synthetic widget coverage (no false alarm, but a real gap).
- **Acceptance:** Every live-widget customer has a monitored URL; a deliberately-broken embed raises a critical email.
- **Status:** OPEN (ongoing; serra-honda done).

### Automated daily DB backup — NEW (added with the Sentinel digest)

- **Background:** There was no automated backup of the per-profile SQLite databases. The Sentinel daily digest now performs one (`src/server/sentinel-backup.ts`, `runDailyBackup`): a consistent `VACUUM INTO` snapshot of every profile DB (brain.db, messaging-hub.db, state.db, response_store.db) into `~/.hermes/backups/<stamp>/`, retention 7, reported (count/size, green/red) in the digest email.
- **Verified live:** in-container run wrote 60 DB snapshots (~19.5 MB) across all profiles; digest email sent (`digestSent=true`).
- **Status:** DONE (main `99df70fdd`). Follow-ups (low-pri): backups live on the same volume as the data — consider off-host/object-storage copy for disaster recovery; retention/cadence tunable.

### Sentinel digest cadence/uptime semantics (note)

- **Background:** Digest is once per 24h (rolling, keyed on `last_heartbeat`), not at a fixed clock time. "Uptime" is `os.uptime()` (host kernel uptime, shared by the container), not app-process uptime. CPU% is load-per-core×100 (approximate), not instantaneous scheduler %.
- **Status:** OPEN (note; tune to a fixed send time or app-uptime if desired).

### Sentinel finding-noise — TUNED to live reality (resolves the earlier noise debt)

- **Background:** First live digest showed 12 findings that were almost all test/certification data + a mis-called probe (operator confirmed liveness: notifications+VIN+dashboards live for all stores; widget/conversations live on serra-honda only).
- **Fix (main `586b1fa17`):** per-store `vinHealthCheck` (calls `vin_token_status` with each store's orgId; the old global probe was missing the required orgId and always failed); `threadSlaCheck` only counts unanswered inbound from the last 24h (`SLA_RECENT_MS`); `dataCollectionCheck` only runs for stores with a live conversational channel (widget targets); throttled checks re-emit open findings so they don't auto-resolve between runs.
- **Verified live:** post-deploy production pass = 193 checks, **0 open findings, 0 errors**.
- **Status:** RESOLVED. Supersedes the earlier "finding-noise tuning" debt entry.

## Full-system functional audit (2026-07-10) — serra-honda live

Read-only audit of the running production system (container image `2b805de3c`). Evidence-backed; five parallel verifiers + a live headed widget sweep. Working-well items (autonomous SMS reply firing in seconds, 0 stuck holds, STOP honored, comms-gate fail-closed, grounding filters draft/grounds canonical, Guardian holds unbacked inventory/specs, Sentinel 15 checks running + alerts firing + daily backup, InfoStore CSV upload + auth sound, workspace/studio render parity) are confirmed and NOT repeated here. New debt:

### [CRITICAL] Dealer lead-notification email delivery ~62% failure

- **Background:** serra-honda `lead-notify` emails in `brain/brain.db comms_log` are **110 error / 66 ok all-time (62.5%)**, ~67% last 7d. Every real lead (SMS, website chat, video, voice) fans out to 6 recipients via `Promise.all` (`src/server/lead-notifications.ts:846-857`). Live-reproduced during this audit: a single website-chat fan-out delivered only to `dwood@` and errored on `leads@serrahonda.co`, `duane.wells@`, `durran@`, `victoria@`. Last-7d per recipient: `victoria@misscommunicationconsulting.com` 9err/0ok (dead), `dwood@serrahonda.net` 8err/1ok (near-dead), `duane.wells@huminic.ai` 7err/2ok, `durran@cageautomotive.com` 6err/3ok, `leads@serrahonda.co` (DMS/ADF intake) 4err/5ok, `sdew@serrahonda.net` (BDC) 2err/7ok (best). Mixed root cause: (a) two addresses (victoria/dwood) fail almost always → likely genuine deliverability/address issue; (b) the 6-way near-simultaneous Resend fan-out is partially rejected per-burst → consistent with Resend per-second rate-limiting / central-mcp transient SSE failures. The failure reason is NOT persisted — `comms_log` has no reason column (`src/server/comms-log.ts:38-64`); `sendViaResendRaw` returns a `reason` that is dropped before recording.
- **Outcome:** The dealer receives only ~1/3 of lead notifications; two configured contacts get essentially none; the DMS/ADF intake (`leads@serrahonda.co`) drops ~half of CRM-bound leads. Direct revenue/complaint risk — leads exist in the hub but never reach the people/CRM. No auto-retry; no operator alert (see next).
- **Acceptance:** Lead-notify delivery >98%. Fix serialize-with-backoff or per-recipient send (not one 6-way Promise.all), persist the provider error reason in `comms_log`, verify victoria@/dwood@ addresses, and confirm `leads@serrahonda.co` ADF intake reliably lands.
- **Status:** OPEN — highest priority.

### [HIGH] Sentinel does not catch the ongoing notification failure (1h blind spot)

- **Background:** `notificationsDeliveryCheck` only looks back 1h (`COMMS_ERROR_WINDOW_MS`, `src/server/sentinel.ts:282`). Leads arrive in bursts hours apart, so a 2+week-long 62% failure has never raised a Sentinel finding or alert. Compounded by the missing error-reason column, the monitoring is blind exactly where the harm is.
- **Outcome:** A revenue-affecting failure runs silently.
- **Acceptance:** A rolling delivery-rate check (e.g. last-24h error ratio > threshold ⇒ warn/critical) that would have caught this; alert wired to `SENTINEL_ALERT_EMAIL`.
- **Status:** OPEN.

### [HIGH] Video (Tavus) + Voice (Vapi) run on static prompts, NOT the live wiki

- **Background:** `autonomous-grounding.ts` grounds only the TEXT widget + SMS path. The Tavus video persona `p9eb007721f4` carries a static `context`/`system_prompt` frozen 2025-11-11; the Vapi assistant `Caroline - Serra Honda` (`90a876c0-…`) has a self-contained static prompt with 0 tools. Wiki edits (inventory, hours, pricing, promos) never reach video or voice. Both webhooks (`api/webhooks/tavus.$profile.ts`, `vapi.$profile.ts`) also treat a missing `TAVUS_WEBHOOK_SECRET`/`VAPI_WEBHOOK_SECRET` (both absent in the profile `.env`) as "no auth required" → forgeable leads.
- **Outcome:** Video/voice Caroline can give stale Nov-2025 answers; webhooks accept unsigned POSTs. Also: STT mis-hears "Serra" as "Sarah" in call summaries; 18/40 video sessions are "(empty video session)" yet each fires a "new lead" notification.
- **Acceptance:** Video/voice ground on the live wiki (or a synced context); webhook secrets set; empty-session notifications suppressed.
- **Status:** OPEN.

### [HIGH/UX] InfoStore upload succeeds but ingested report data is not displayed

- **Background:** `REPORT_METRICS_TRUSTED = false` (`src/server/crm-guru.ts:53`, set 2026-06-22 for a real ~1.8–2× report-vs-CRM window mismatch) suppresses all report-derived dashboard metrics (`crm-guru.ts:257-259`). serra-honda has 33 ingested 2026-07-07 `report_lead_source_roi` rows sitting in the Brain, but the dashboard shows "needs supplemental data" for Contacted/Appointments/timing/gross; only live-API Leads + Sold render. A partner who uploads a correctly-named CSV sees "upload success + row count" while the dashboard stays blank — expectation mismatch. PDF uploads are stored but never parsed (CSV-only ingest), with no strong in-flow "PDF not parsed" message.
- **Outcome:** Dealer expects to see their uploaded data and does not.
- **Acceptance:** Retire the blanket flag → period-scoped trust with a variance guardrail (planned InfoStore reporting-ingestion phase); PDF parsing or explicit "export as CSV" guidance in the upload UI.
- **Status:** OPEN — already scoped as the next goal phase (A–E).

### [P2-9 — MATERIALLY FIXED 2026-07-10 via VIN-authoritative data]
- **Root cause (found via `vin_list_users`, the dealer's own CRM):** notification routing used the WRONG DOMAIN. All 34 serra-honda VIN users are `@serrahonda.co`; the routing had `dwood@serrahonda.net` (Don Wood, GM) which failed 4/28.
- **FIXED:** `dwood@serrahonda.net` → `dwood@serrahonda.co` (his authoritative VIN email; valid MX; live studio.yaml on the volume, backup `.bak-p29-dwood-*`; takes effect next lead). Dealer's own recipients now all deliver: `leads@serrahonda.co` (DMS/ADF), `dwood@serrahonda.co` (GM), `sdew@serrahonda.net` (BDC, 7/9 works — left, don't break working). Rate-limit component already fixed by P0-2.
- **OPERATOR CLARIFICATION (2026-07-12) — the notification list is operator-owned + redundant BY DESIGN.** The multi-recipient fan-out (incl. the `adf-xml` → `leads@serrahonda.co` DMS entry, which VIN then distributes from) is intentional and standard across all dealers. If an address is wrong/failing, the agent's responsibility is only to **ADVISE**; the operator edits the list. Do NOT redesign routing or remove recipients. (I earlier over-concluded that the human fan-out was redundant "rot" and proposed ripping it out — that was wrong; corrected here.)
- **ADVISORY to operator (for the operator to edit the list):** three entries currently fail — `victoria@misscommunicationconsulting.com` (1/33, 97% fail, dead mailbox; not a VIN user); `durran@cageautomotive.com` (12/33, 64% fail; VIN has `dcage@serrahonda.co` for the same person); `sdew@serrahonda.net` (wrong domain; VIN has `sdew@serrahonda.co`). Also note historical `dwood@serrahonda.net` failures predate the earlier `.net`→`.co` fix.
- **MONITORING DELIVERED (2026-07-12, operator-greenlit) — Sentinel now auto-advises which entry is bad.** New per-recipient health check `notificationRecipientHealthCheck` (`sentinel.ts`) + helper `countCommsByRecipient` (`comms-log.ts`, unnests the `recipients` JSON via `json_each`): flags any single recipient with ≥6 email sends and ≥50% failure over 24h (≥90% → critical), naming the exact address, while leaving healthy co-recipients (e.g. `leads@serrahonda.co` DMS intake) alone. So a dead/mistyped list entry surfaces by name for the operator to fix, instead of being hidden by the aggregate rate. **pnpm test (full 1152 pass) + pnpm build green; independently Checker-verified** incl. reproducing the live query (victoria@ 97%→CRIT; leads@ 24%→not flagged); monitoring-only, zero studio.yaml/routing edits, no hardcoded addresses. DEPLOYED to production 2026-07-13 (PR #84, merge `7bffc9702`) + Checker-verified live (flags the known-dead victoria@ at 97%, leaves the DMS intake alone).
- **Status:** dealer-facing delivery FIXED (P0-2 mechanism + dwood domain); bad-address DETECTION now automated (Sentinel, pending deploy-GO); the actual list edits (victoria/durran/sdew) are the operator's per the clarification above.

### [MEDIUM / P1-5-followup] Report total_gross has no lead-count baseline (un-guarded financial number)
- **Background (Checker-found 2026-07-10):** the P1-5 variance guardrail validates the 3 lead-COUNT stages (Contacted/Appts-Set/Appts-Shown/Good-Leads must be <= live Leads). But `lead_sources[].total_gross` ($ figure, `crm-guru.ts:364`) is NOT in the guard — it has no live lead-count to validate against. So on a report whose COUNTS are consistent (passes the guard), a wrong/inflated gross-dollar could still display unchecked.
- **Current status:** DORMANT — `REPORT_METRICS_TRUSTED=false` (reverted to safe 2026-07-10, PR #82), so no report metric (incl. gross) displays at all. The gap only matters when P1-5 is re-enabled.
- **Fix before re-enable:** either give total_gross its own sanity rule, or SUPPRESS report gross entirely (`total_gross: null`) until a validated ingester exists (Checker's recommended option). Do NOT re-enable `REPORT_METRICS_TRUSTED=true` until this is decided + the guard is verified per-store against the TRUE live 30-day window (Checker verified serra-honda: report 334 vs live 306 → correctly suppresses).
- **Status:** OPEN — tracked (P1-5-followup). Non-blocking while the flag is false.

### [MEDIUM] Sales-conversation style applied to SMS only, not widget/video/voice

- **Background:** The coached style (champion the team; assumptive either/or, never yes/no; never "I can't"; own the next step) lives in `governance/agents/caroline/personas/sms.md` + canonical wiki play nodes. The public web widget uses its own prompt path (`widget-chat.ts`) and does not load `sms.md`. Live-verified: to "Can you get me a 2026 Prologue in silver? monthly payment?" the widget replied grounded + safe (no fabricated price/inventory) BUT asked a yes/no question ("Would you like me to have a salesperson reach out?") and led with "I won't be able to…" — the exact anti-patterns removed from SMS. Video/voice use static prompts (see above).
- **Outcome:** Inconsistent brand voice across channels; the web widget uses the un-coached style.
- **Acceptance:** Widget (and video/voice) reflect the same five sales rules.
- **Status:** OPEN.

### [CORRECTED 2026-07-10] Failed SMS sends: error IS persisted (audit was imprecise)
- **Verification (live data):** The 4 failed follow-up sends DO persist their reason in message metadata: invalid numbers (Hdud/Jsmith) → `error="textmagic API error: Validation Failed"`; Wyatt/Cynthia → `error="terminated"` (a **transient** central-mcp stream termination, not a bad number). The autonomous-reply path persists it as `adapter_error` (e.g. Durran `adapter_error="terminated"`); the automations path persists it as `error`. So "error not persisted" was FALSE — the audit agent checked the wrong metadata field.
- **Real residual (LOW):** (a) field-name inconsistency (`error` vs `adapter_error`) — cosmetic; (b) transient `"terminated"` SMS sends are not retried, so 2 of the 4 (possibly real customers) silently missed their follow-up. Adding retry to the CORE SMS send path was deliberately NOT done overnight (double-send risk on a mid-flight 'terminated'); flagged for a supervised change. NOT a P2 blocker.
- **Status:** RECLASSIFIED — not the bug it was logged as. Original entry below retained for history.

### [MEDIUM — superseded by correction above] Failed SMS sends: no retry, no alert, error not persisted

- **Background:** 4 `automation_runs` (24h-follow-up) failed ~33-35h ago — TextMagic rejected the send (handles +16662224455/Hdud, +18438222299/Wyatt, +16767205676/Jsmith, +13347475359/Cynthia; two are invalid NANP area codes 666/676; names look like test data). None are opt-outs. The live/automation reply path does not persist `res.error` (`agent-autonomous-reply.ts:493`), so `metadata.error=undefined` — root cause not recoverable from the DB, and no operator alert on `adapter_status=failed`.
- **Outcome:** If any failed recipient is a real buyer, they got silence, invisibly.
- **Acceptance:** Persist provider error on failed sends; surface repeated failures to a human; optional retry for transient errors.
- **Status:** OPEN (low customer impact here — likely test data — but a real gap).

### [CRITICAL] Website chat → VIN lead: no name, no phone, duplicate leads (dealer-reported)

- **Background:** Serra employee reported (2026-07-10) that testing the AI lead via website chat → VinSolutions: (1) it never asks for a name, (2) it does not attach the phone number even when the customer provides one, (3) it creates **2 leads from one chat**. Code trace: `src/routes/api/public/widget-chat.ts:263-273` fires `notifyNewLead({channel:'website chat', contact_handle: chatHandle, display_name null})` on thread creation; `chatHandle` is an anonymous session id (`chat:<uuid>`), and the ADF-XML lead emitted to `leads@serrahonda.co` (VinSolutions intake) is built from that handle — the customer's typed name/phone in the chat body is never parsed into the ADF `customer.first_name`/`phone`. **Live-confirmed:** thread `6f9264ca` — customer typed `678-492-1396` in chat, Caroline acknowledged, but the ADF lead carried no phone. Each hub thread logs exactly 1 `Lead notification`, so the duplicate is on the VIN/ADF side (candidate causes: ADF email path + a second create path, or a retry, or VinSolutions parsing one email as two) — must be reproduced via `vin_query_leads` and fixed to exactly 1 lead per chat.
- **Outcome:** Every website-chat lead reaches the dealer's CRM anonymous and un-callable, duplicated — sales cannot follow up. Direct, dealer-visible, revenue-impacting.
- **Acceptance:** Chat captures name + phone (persona asks; parser extracts a phone from the chat body) and populates the ADF `customer` block; exactly ONE VIN lead per chat conversation; verified via `vin_query_leads` after a controlled test.
- **Status:** OPEN — dealer-reported, high priority.

### [HIGH] The 4 non-honda store widgets are LIVE but ungrounded (completeness-pass addition)

- **Background:** Sentinel `grounding:no-canonical` for serra-nissan, tony-serra-ford, ford-of-columbia, hyundai-of-columbia (+ serra-service). Each has a live public chat widget but no canonical company-wiki, so `recallCompanyWikiTop` returns nothing and the widget answers persona-only — store-fact-blind and fabrication-risk. serra-honda is the only seeded store.
- **Outcome:** Real customers chatting with the other stores' widgets get generic/ungrounded answers; a fact-ask has no anti-fabrication grounding backstop the way serra-honda does.
- **Acceptance:** Each live store widget either grounds on canonical store facts (seed via the serra-honda method) or is intentionally gated until seeded. Operator decision per store.
- **Status:** DONE (2026-07-10) — seeded 9 universal store-neutral CANONICAL nodes to all 4 stores via guardedWikiWrite (`scripts/seed-stores-universal-p16.ts`, PR #77 + #79) + brand-fidelity guard in the widget prompt (PR #78). Checker-APPROVED after catching + fixing a Honda leak (CR-V / serra-honda-sales-chat / Serra program names). Live-verified: grounding parity (top=30 all 5), corpus grep 0 Honda/serra-honda refs, ford-of-columbia widget refused a Honda CR-V and named only Ford models. Anti-fabrication intact; autoreply OFF (widget-only, no new sends).

### [LOW / tracked debt] Pre-existing PUBLISHED nodes in the 4 stores carry false Honda brand-identity (P1-6 followup)
- **Background (Checker-found 2026-07-10):** ~13 Honda refs remain in `status: published` (NOT canonical) nodes on the 4 non-honda stores — most notably `company-overview.md` "<Store> is a full-service **Honda** dealership" (factually false for Ford/Nissan/Hyundai), plus process/team/glossary/welcome/recalls nodes. These were seeded by a prior partial-genericization pass.
- **Why non-blocking:** `autonomous-grounding.ts:60` requires `status==='canonical'` to ground, so published nodes do NOT reach the widget today. Latent, not live-reachable.
- **Why still real debt:** becomes live the instant someone promotes such a node to canonical, or a recall path widens to published, or a human reads the store wiki. A brand-identity falsehood.
- **Fix (careful, NOT a blind pass):** genericize the published start-here/team nodes per store — BUT these may also carry serra-honda FACTS (address/phone/hours) copied over; those need the real per-store facts (operator/dealer), not just a name-swap. Do not invent them.
- **Status:** OPEN — tracked (P1-6 followup / same bucket as P3-13 data-hygiene). Non-blocking.
- **PARTIAL RESOLUTION 2026-07-11 — real per-store hours/location/contact now grounded (all 5 stores).** Operator authorized sourcing facts from each dealer's website. Read authoritative hours/address/phone from EACH store's own site via headed Playwright browser (bypasses the WebFetch 403), cross-checked against each store's Vapi assistant config — **the Vapi configs had errors** (Tony Serra Ford carried Honda's address+phone; Ford of Columbia had wrong address 1229 vs real 1350 + wrong phone), so the website was used as authoritative. Seeded canonical `company-wiki/dealership/hours-location-contact.md` to all 4 non-Honda profiles (`system:p16-hours-seed`, action=create): tony-serra-ford (40905 Hwy 280 Sylacauga; Sales M–Sa 9–7; Svc M–F 7:30–5 Sa 7:30–1; Parts M–F 7:30–5:30 Sa 8–1), serra-nissan (80 James Payton Blvd; Sales M–Sa 9–7; Svc/Parts M–F 7:30–5:30 Sa 8–1), ford-of-columbia (1350 Nashville Hwy TN; Sales M–F 9:30–7 Sa 9–6; Svc M–F 8–6 Sa 8–1; Parts M–F 8–5 Sa 8–1), hyundai-of-columbia (1370 Nashville Hwy TN; Sales M–F 9:30–7 Sa 9–6; Svc/Parts M–F 8–6 Sa 8–1). All Sun closed. **Independently Checker-verified**: Checker re-read all 4 websites itself, every fact matches, zero discrepancy/fabrication, grounding HIT + grounded=true per store. Combined with serra-honda (P3-10), the **5-store hours matrix is grounded + verified**.
- **Recall nuance (low):** the very specific phrasing "what time does service close Saturday" ranks the hours node 4th (behind keyword-competing sales-play nodes) for the 4 non-Honda stores, so that exact query may route-to-human instead of answering (grounded=true either way — never fabricates). The serra-honda node answers it (richer "how to answer" body). Enriching the 4 nodes to match would need the inbox-proposal path (they are now canonical → `canonical-frozen`). Primary hours questions (open Sunday / sales hours / general hours) all HIT.
- **Repo followup DONE (2026-07-11):** added 4 fixtures `scripts/seed/<store>/company-wiki/dealership/hours-location-contact.md` (byte-identical to the live nodes — diff-verified) + idempotent ESM seed script `scripts/seed-store-hours.ts` (`--dry-run`; honors `BRAIN_PROFILES_ROOT`; SKIPs a node already on the volume to avoid `canonical-frozen`). Verified: dry-run lists all 4; live run SKIPs all 4 (already present). Needs a PR to land in `main` (working tree only; not committed). NOTE: full `pnpm build`/`pnpm test` not re-run this turn — the change is markdown fixtures + one standalone tsx script mirroring `seed-serra-honda-knowledge.ts` (ran clean via tsx), so app build/test are not expected to be affected; run the formal gate before merge.

### [LOW] serra-honda wiki-orphan warnings + hours node still draft

- **Background:** Sentinel shows 3 serra-honda `warning` findings: `company-wiki/sales/conversation-approach.md` references node ids `agent.caroline.role`, `policy.guardrails`, `sales.lead-followup-cadence` that don't resolve (introduced by the current branch's persona/wiki edits). Separately, `dealership/hours-location-contact.md` is still `draft`, so simple "are you open Sunday?" asks route to a human instead of being answered (Guardian won't hold hours; it's not inventory/specs).
- **Outcome:** Grounding-integrity noise; a benign-ask miss on hours.
- **Acceptance:** Fix/rewrite the dangling relationships in `conversation-approach.md`; operator verifies + promotes the hours node to canonical.
- **P3-10 RESOLVED 2026-07-10 (operator authorized sourcing facts from the dealer website).** The authoritative hours were read directly from `serrahonda.net` via a **headed Playwright browser** (a real browser renders the pages that returned HTTP 403 to plain `WebFetch`). Per-department hours confirmed to DIFFER (I did not assume): **Sales** Mon–Sat 9:00 AM–7:00 PM / Sun closed (`/dealership/directions.htm`); **Service** Mon–Fri 7:30 AM–5:00 PM, Sat 8:00 AM–1:00 PM / Sun closed (`/service/center.htm`); **Parts** same as Service (`/parts/center.htm`); address **40941 US Highway 280, Sylacauga, AL 35150-6812**; phone **(256) 245-5000** (all depts). The node `company-wiki/dealership/hours-location-contact.md` was rewritten with these facts (source_of_truth = the three dealer URLs + read date) and promoted to **`status: canonical`** on the live serra-honda volume via `guardedWikiWrite` (`--promote`; backup `hours-location-contact.md.bak-p310-*`). **Grounding verified live** (`recallForAgent` returns the node for "are you open sunday / what time does service close / sales hours"). **Independently Checker-verified** — the Checker re-read all three website pages itself and confirmed every department's hours/address/phone match the node exactly, zero discrepancy, no placeholder text.
- **Second authoritative source (2026-07-10):** the dealer's own **Vapi assistant config** ("Caroline - Serra Honda", read via `vapi_list_assistants`) independently CONFIRMS the same DEALERSHIP INFORMATION — address 40941 US Highway 280 Sylacauga AL 35150-6812, phone (256) 245-5000, Sales Hours Mon–Sat 9:00 AM–7:00 PM/Sun closed, "Sales opens 9:00 AM / Service opens 7:30 AM". Two independent dealer sources (website + Vapi) agree; the website was the fuller source (added Service/Parts close times + Saturday hours the Vapi prompt omits). Note: each OTHER store also has its own Vapi assistant (Georgia-Tony Serra Ford, Magnolia-Serra Nissan, Savannah-Ford of Columbia, Elizabeth-Hyundai of Columbia) whose prompt likely carries that store's real facts — a readable source to ground the 4 non-Honda stores' hours/address/phone (see P1-6 followup).
- **Repo followup (non-blocking):** the repo fixture `scripts/seed/serra-honda/company-wiki/dealership/hours-location-contact.md` was updated to the canonical content in the working tree; needs a PR to land in `main` so a future re-seed carries the real hours. Live volume is already correct.
- **Status:** **Hours node (P3-10) = DONE + Checker-verified.** wiki-orphan warnings (P3-9 dangling relationship ids in `conversation-approach.md`) = OPEN (low). **Update 2026-07-10:** the live node is `status: canonical`; attempting to re-seed the corrected relationship ids (`agent.caroline.role`→`serra-honda/agents/caroline/role`, etc.) via `guardedWikiWrite` is **intentionally blocked by the KSG `canonical-frozen` gate** ("Open an inbox proposal instead") — a governance guardrail working as designed. Did NOT bypass it (CLAUDE.md: do not work around gates; no silent changes to governance). Sanctioned fix = operator-console / inbox-proposal edit of the canonical node, OR the gate would allow it if the node were reverted to draft first. Fixture already carries the correct full-form ids for any fresh seed. Low severity: relationship-graph metadata only; the node still grounds and is not customer-facing.

### [P1-4 / P3-11] Video (Tavus) + voice (Vapi) grounding, webhook secrets, STT keyterm — provider-side, no code path
- **Background (verified against code 2026-07-10):** Both webhook handlers — `src/routes/api/webhooks/tavus.$profile.ts` and `.../vapi.$profile.ts` — are **end-of-conversation RECEIVERS ONLY**. They record the transcript/summary AFTER the call/session ends and fire a lead notification (`dispatchLeadNotification`). Neither generates the spoken/video reply: the conversational reply is produced **provider-side** (Tavus replica persona; Vapi assistant). There is therefore **no line in our codebase that emits a video/voice reply** to attach wiki-grounding to — unlike the web widget (`widget-chat.ts`, which we DO ground). Grounding video/voice requires editing the Tavus persona / Vapi assistant system prompt, which is provider-dashboard config. The central-mcp toolset exposes only read/list tools (`tavus_list_personas`, `vapi_list_assistants`) — **no `tavus_update_persona` / `vapi_update_assistant`**, so it cannot be done via API either.
- **Webhook secrets (P1-4 "secure" half):** `readSecret(profile)` reads `TAVUS_/VAPI_WEBHOOK_SECRET` from per-profile `.env`; when unset the webhook accepts unauthenticated POSTs. Setting a secret **without** configuring the matching header provider-side would break the webhook (401 on every real event) — cannot be half-applied; needs provider-side coordination.
- **P3-11 (Vapi STT "Serra"→"Sarah"):** the keyterm/vocabulary hint is Vapi assistant transcription config — same provider-dashboard surface, no API tool.
- **Impact:** video/voice channels answer from the provider persona prompt, not the local canonical wiki, so they lack the widget's anti-fabrication grounding backstop; Vapi may mis-transcribe the brand name. Webhook auth is open where no secret is set.
- **Acceptance:** With Tavus/Vapi dashboard access (or update-capable keys), (1) inject the same grounding/anti-fabrication guidance into the persona/assistant prompt, (2) set webhook secrets on both sides together, (3) add the "Serra" keyterm; verify with one controlled test call/session.
- **Status:** DEFERRED TO A LATER PHASE by operator (2026-07-10): "there is a prompt [in Tavus/Vapi] which is staying for now until we move on to later phases and wire MCP and infostore into Vapi and Tavus so they can read it directly." So P1-4 grounding + P3-11 keyterm are intentionally parked until that MCP/InfoStore-into-provider wiring phase — NOT an open remediation blocker. The current provider persona/assistant prompt stays as-is for now. Code finding above (webhooks are receivers; no update API) confirms there is no in-repo path until that wiring exists. Revisit with the paused subsequent phases ([[paused-phases-resume-after-remediation]]). Related: reachability/credit-balance monitoring debt (lines ~216–220).
