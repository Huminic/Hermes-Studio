Build and prove the repeatable Honda-first Semantic Watchdog for Serra Honda of Sylacauga (`serra-honda`, dealer `21043`) across authoritative `SW-001..SW-295`; never shrink completion to the measured subset.

Read and follow:
- `docs/halo/planning/HONDA_SEMANTIC_WATCHDOG_EXECUTION_SPEC.md`
- `docs/halo/contract/semantic-watchdog-feasibility-matrix-295.json`
- `/home/ubuntu/hs-ingest-dev/docs/watchdog-platform/SCHEMA_CONTRACT.md`
- `/home/ubuntu/hs-ingest-dev/docs/watchdog-platform/SCHEMA_CONTRACT_BROWSER_EXTENSION.md`

Permanent boundary: VinSolutions read-only; Honda Sales only. Service/Parts belong exclusively to the combined Serra Service workspace and may not enter Honda Sales sources, metrics, grades, narrative, or findings. Preserve pre-project report circuits. Work in dev only. No schedule, CRM/customer, production, recipient, compliance/PII, or customer-commitment change without authority. Missing is not zero; never infer around absent data.

Execute one source family and one module at a time. First pin implementation/schedule truth and create the exact 295→11 map, per-metric dependency/disposition registry, baseline contract, extraction schemas, and rollback points. Claude owns dev code, InfoStore, modules, reports, and tests. Codex owns read-only acquisition, immutable evidence, admission, reconciliation, acceptance, and PDF QA. Claude must not browse Vin/Gmail or manufacture data. An impartial non-author/deployer reviews pinned evidence.

Acquire each proved Honda dataset once per family/period/schema: six native XLSX families, implemented Response Times, and field-minimized Custom Reporting/browser sources proved in discovery. Sales-clean raw bytes enter immutable Honda landing; contaminated ROI/CAGE/Communication bytes enter only the segregated pre-admission vault outside Honda Brain. Hidden Service/Parts cannot be cured by clean rows. Unreliable browser families stay unavailable until contracted. Register external, history, compliance/PII, cross-rooftop, and Service dependencies; never substitute.

Persist every metric’s exact status and boundary class (`sales`, separate Service, cross-rooftop, compliance/PII, external). Measurable metrics require direct fields, formula, numerator/denominator, period, immutable lineage, confidence, and separate versioned detection threshold, comparison reference, and grade target. Use compatible dealer history, capability normalization, published benchmarks, or Duane’s labeled manual potential. Never fabricate a grade, cause, units, or ROI. Content metrics require the spec’s authorized protected/reproducible NLP envelope or remain pending.

Complete each module with raw→normalized and normalized→metric/grade/narrative proofs. Persist observations, evaluations, module runs, findings, and report lineage. Every module partitions IDs into measured, disposition-only, and rejected; grades/narrative use measured only, while the safe appendix covers all 295. Produce QA’d subsections and a customer PDF with high-level/section narratives, strengths, weaknesses, actions, and supportable deduplicated opportunity ranges. Keep quarantine detail internal.

Acceptance: exact 295/295 dispositions/boundaries; every obtainable authorized Honda Sales metric acquired, processed, persisted, evaluated/interpreted, independently tested, and graded only when `gradable=true` with an approved compatible target; unavailable claims evidenced; traceable grades/findings; zero Service/Parts admission; second-period/replay proof of idempotence, trends, and reproducibility; unsent alert simulations; final PDF, internal companion, and runbook; no skipped source, file, metric, or gate. No Nissan/Ford, production activation, alert delivery, or customer email.
