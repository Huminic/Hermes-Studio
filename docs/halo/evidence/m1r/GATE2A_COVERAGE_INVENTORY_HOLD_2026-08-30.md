# M1R/M2R Gate 2A — Coverage Inventory HOLD

Date: 2026-08-30 EDT  
Controller: Codex  
Bounded analyst: Claude Code  
Independent auditor: Shadow (`/root/shadow_auditor`)  
Result: **HOLD / incomplete; no downstream gate authorized**

## Bounded objective

Read governing sources and produce a no-write inventory for the six native VinSolutions families across `serra-honda` (`21043`), `serra-nissan` (`21044`), and `tony-serra-ford` (`21047`), plus browser-only, external, derived, missing, and readerless requirements. This was the read-only evidence phase before writing a Gate 2 contract.

## Planned versus actual

Claude preserved the isolated clone at `dev/ingest-endpoint@4c41df11dc48c3bc954ffdd45cdf125b2d67c2d5`, clean and unlocked, and re-proved the original shared-repository status digest `7c88f42043894ca259e881926b9afc97b10a62f5aadaae336c63bf99451356da` unchanged. It read the control directive, M1/M2 records, promotion evidence, milestones, schema contract, and the 19-ID runtime alert-picker catalog.

Claude then stopped on its context/token limit before completing the 295-condition catalog, 18 native cells, browser/external register, reader/calculation/test inventory, or dual-proof report. It correctly did not claim PASS. No Gate 2 contract file, code, data, Git ref, schedule, browser, VinSolutions, CRM, Service, or Parts object was changed.

## Shadow corrections required for resumed Gate 2A

1. The 19 IDs in `/home/ubuntu/hs-watchdog/src/server/watchdog/metric-catalog.ts` are a curated executable alert-picker subset, not the full Semantic Watchdog catalog.
2. The authoritative feasibility matrix is the JSON array at `/Users/duanewells/Documents/LifePath 2/outputs/semantic-watchdog-crm-feasibility-2026-08-20/semantic-watchdog-feasibility-matrix.json`, SHA-256 `29c7ac06130f9b4fe8d5df0a2d0d6fffed7c6ff4dc02eca96e0f44d109a04fc1`, length exactly `295`.
3. Its classification totals reconcile exactly: native scheduled `20`; scheduled plus calculation/NLP `162`; external `56`; native manual export `7`; unavailable/retention-limited `8`; manual CRM inspection `7`; outside governed boundary `35`; total `295`.
4. This LifePath-located file is read-only source provenance only. Gate 2 must establish the project-owned authoritative copy/contract under the Halo repository so the runtime project does not depend on the LifePath workspace. No LifePath mutation or LifePath validator is part of this project.
5. The operator taxonomy at `/home/ubuntu/Claude-store/huminic-studio/uploads/semantic-watchdog-catalog.md` is a distinct canonical reference layer, not interchangeable with either the 295 feasibility matrix or the 19-ID runtime subset. Its declared counting method and reconciliation to the original operator uploads must be explicit.
6. `gross.*` is not one uniform current source: isolated ingest `vin-metrics.ts` computes gross metrics from `crm_sales_gross`, while `hs-watchdog` currently derives only `gross.total_sum` from `dealership_performance` and withholds reconciliation pending per-deal gross.
7. Service-specific catalog items remain catalogued but unavailable/out of scope for the three Sales profiles. They may not be silently dropped, zeroed, or routed into Sales.

## Proof delta A — scope/state

- Isolated clone remained clean, unlocked, and on the exact pre-state commit/branch.
- Original shared-repository digest and HEAD remained unchanged.
- Approved frozen process groups remained stopped; no executable concurrent writer appeared.
- No file or external-object write occurred in Gate 2A.

## Proof delta B — independent outcome finding

Shadow independently proved the 295-condition source, exact hash, exact array length, and exact classification reconciliation, and rejected the incomplete 19-metric total. Shadow also independently identified the split gross-source behavior. These findings prevent a false coverage claim and define the resumed pass.

## Safety result

Sales-only boundary preserved. No Service/Parts data entered any Sales source, saved definition, transformation, metric, or narrative. No production, browser, schedule, CRM, message, or customer/dealer action occurred.

## Next bounded action

Preserve and replace the exhausted Claude session, then resume the same read-only Gate 2A with the exact 295 source, separate taxonomy layers, gross-source distinction, 18-cell matrix, browser/external register, and code/test inventory. Gate 2B remains unauthorized until the resumed inventory and Shadow review are complete.
