# Kanban Notes

Hermes Kanban is a durable task board shared across Hermes profiles and backed by `~/.hermes/kanban.db`. Worker profiles should load worker capabilities, and orchestrator behavior should route multi-step work onto the board instead of relying on fragile inline subagents. See: https://hermes-agent.nousresearch.com/docs/user-guide/features/kanban
